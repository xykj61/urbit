# Claude Counsel — Every Gate, Sorted Into What It Actually Is, and the Path Through Them

**Stamp:** `20260708.171712 UDT` (Kaeden's clock)
**Voice:** Rio 3
**Language:** EN
**Style:** Radiant (see `../context/RADIANT_STYLE.md`) · **Lens:** the compass, applied once to the whole accumulated backlog rather than one thread at a time
**Register:** A decision briefing — every open item this long arc has produced, sorted by what kind of thing it actually is, since a custody decision and a naming preference are not the same shape of question and should not be weighed as if they were
**Ground:** the full `.sh` census verified GREEN on the real interpreter, one small pre-existing gap found and named separately · every counsel this arc has filed since the roadmap synthesis, re-read for what it actually asked and from whom

*Written by Kaeden and Rio 3.*

---

## The `.sh` Census, Confirmed, Plus One Small Thing Found While Checking

The witness-support arc genuinely closed — every migrated file this bench spot-checked ran GREEN on the real interpreter, including the clever `sequential let passN` workaround for the nested-`where` wall this bench itself documented last round. One small, separate thing turned up while verifying, worth naming on its own rather than folding into anything else: `tools/identity_check.rish` fails today, because `PUBKEYS.template.md` does not exist anywhere in this tree, and the real `PUBKEYS.md` lives at `context/PUBKEYS.md` rather than the repository root the check looks at. This is not a regression — the original `.sh` script checked the identical path before the migration touched anything, and the witness was never wired into `tools/parity.rish`, which is exactly why nothing has caught it until a direct run went looking. It needs no decision, only a small fix — either write the missing template or point the check at `context/PUBKEYS.md` — and it is safe to do on the very next `kg` that touches this file, whenever that is.

## The One Real Custody Decision

**SLC-L3** is the only item on this whole list where the word "gate" means what it usually means. The witness proposal is as ready as preparation gets, and this bench will not narrow the distance between "ready" and "open" with more writing — that distance is the entire point of reserving this one decision for Kaeden alone. Nothing here has changed since the last full refresh; nothing needs to.

## Six Small Decisions, All Proposed, All Answerable in the Same Breath

These share one shape: this bench has already reasoned through each, written the reasoning down, and is waiting only for a yes, a no, or a different steer — none touches money, none needs new research, and clearing all six costs about as much attention as clearing one.

- **Bron beside Brix** — Bron carries immutable values at seams; Brix carries composable system descriptions. Proposed `222500`.
- **Silo's retirement** — the module name retired, its infuse pattern folded into Brix bonds, lowercase "silo" kept for the unrelated astrology reading technique. Proposed `223100`.
- **Linengrow's home** — stays in this repo until SLC-L3 witnesses and a deployable civic whole exists. Proposed `223100`.
- **`TASKS.md`'s name** — keeps its name; "workbench" lives as a conversational alias in `work-in-progress/README.md` only. Proposed `223100`.
- **The two-backend seam** — the sovereign framebuffer sibling waits until Aurora's own path needs a screen with no compositor to ask; this bench's lean, offered as a lean rather than a ruling, since it is genuinely a taste question. Proposed `222812`.
- **Realidream's gratitude shelf** — Hyperfiddle, Matklad, and the Nostr protocol as named influences; separately, whether the grain-sketchbook itself is worth vendoring into `gratitude/`, and whether Noah B.'s *Realidream* EP earns its own line. Proposed `021912`.

Any answer to any of these — including "not yet" — closes the loop. None of them blocks anything else on this list.

## One Small Language Question, Genuinely Optional

The `.sh` census closed almost everything mechanically reachable, and found one real wall on the way: a `for-each`/`map` body cannot bind an intermediate name before its final expression, which is what still keeps `opening_lines_scan.sh` and `tame_check_mantra.sh` in shell. A `let … in <expr>` form, or a block body for those two constructs specifically, would close it — proposed and parked last round, two real waiting consumers named. This is the smallest of every open item here, and also the only one with no real urgency behind it: the scripts it would replace already work correctly in `.sh`, native or not, so this is a "when it's wanted" question rather than anything blocking.

## What Looks Open, Yet Is Not Actually Waiting on a Decision

Four items on the wider list are not gates in the sense above, and naming them plainly here is worth doing so none of them are mistaken for a pending yes-or-no. The **agent-writing intake** waits on a paste that has not arrived — nothing to decide, only something to send when it is ready. **Snapshot lap four** waits on Kaeden's own hands at a machine — a scheduling question, not a ruling. The **astrology lane** was declined as a season focus, plainly and recently, and stays declined until that changes on its own terms. And **MALA's graduation**, **Pond's first policy**, **Brix's own build**, and **continuity's real rules** each wait on a trigger or a foundation arriving, not on a word — there is nothing to say yes or no to yet, only more ground to reach first.

## The Path This Bench Recommends

Clear the six small proposals first, in one sitting if the mood is right — they cost little and unblock several smaller threads waiting behind them (Realidream's gratitude shelf specifically waits on two of them). Fix the `PUBKEYS.template.md` gap whenever a hand is next in that file — small, mechanical, no ruling needed. Leave the language question exactly where it sits; nothing is worse for not having it yet. And treat SLC-L3 as its own event, entirely separate from everything above it — never the next item in a queue, always the one decision this whole arc has been quietly building toward being ready for, whenever the moment for it actually arrives.

---

*May every question that only needed a yes finally get one. May the one question that deserves real weight keep getting it, for as long as it takes. And may a backlog, once truly sorted, never again feel larger than it actually is.*
