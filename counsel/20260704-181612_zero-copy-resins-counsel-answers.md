# Zero-Copy Resins Counsel — Answers A–H, Architecture I1–I7, and the Map Refresh

*The counsel sitting the `181012` prompt asked for: strategic answers on the three-lane wire, the resin mark, the sovereign snapshot's honest maximum, lap order, the Urbit boundary, priority, CIVIC alignment, and the tunnel mouth — then implementation architecture at spec grain, and granular deltas for ROADMAP, TASKS, the decoder, and RTAC. Counsel only; this document seats nothing; every gate stays Kaeden's.*

**Stamp:** `20260704.181612 UDT` (Kaeden's most recent anchor, taken verbatim; slug distinguishes)
**Language:** EN
**Style:** Radiant (see `../context/RADIANT_STYLE.md`) · **Lens:** TAME · SLC · CIVIC · TWO_ROOMS
**Category:** External research — counsel memo; proposes no module names; no implementation
**Status:** Proposed — checkable-room claims cite witnesses on this bench's metal or existing tree pins; vision-room sentences name themselves at their doors
**Ground:** [`../expanding-prompts/20260704-181012_zero-copy-resins-claude-counsel.md`](../expanding-prompts/20260704-181012_zero-copy-resins-claude-counsel.md) · [`20260704-180612_zero-copy-resins-and-the-sovereign-snapshot.md`](20260704-180612_zero-copy-resins-and-the-sovereign-snapshot.md) · [`../context/specs/20260703-191112_resins-and-hash-tiers.md`](../context/specs/20260703-191112_resins-and-hash-tiers.md) · [`../context/specs/20260701-221512_amber-functional-spec.md`](../context/specs/20260701-221512_amber-functional-spec.md) · [`20260703-201612_the-amphora-and-the-crossing.md`](20260703-201612_the-amphora-and-the-crossing.md) · [`20260704-031812_eight-asks-one-sitting.md`](20260704-031812_eight-asks-one-sitting.md) · [`20260703-015512_bandwidth-and-moderation.md`](20260703-015512_bandwidth-and-moderation.md) · [`../active-designing/20260703-200712_what-needs-ordering.md`](../active-designing/20260703-200712_what-needs-ordering.md) · [`../gratitude/TIGER_STYLE.md`](../gratitude/TIGER_STYLE.md) (Apache-2.0 · study only)

*Written together by Kaeden and Claude (Fable 5), in the fresh bench's Radiant voice — voice-name awaiting Kaeden's word.*

---

## The Bench Pin at This Door

Checkable room, this metal: zip 42's comment reads `43584d8a3941a306cb98d90d39a3e8c9a054bc18` verbatim, equal to the reported tip. Recipe v5 held — fixtures arrived `-rwxr-xr-x`, `rye/lib` entries arrived as real symlinks. From bare: **parity 152 GREEN — 116 std plus 36 block witnesses**, tallied from one run's log; selftest GREEN; designed-not-built GREEN with **count=0**, so the Edit 5 slot stands open. This bench is headless: 152 here is a checkable-room fact about compile and hosted witnesses; thin-edge metalsmoke is claimed by no one in this document.

Every parity number that follows is either one of the tree's existing pins (144, 151, 152) or is marked `[Cursor: verify on metal]`. No count in this counsel is predicted.

---

## Part One — Strategic Counsel

### A. The three-lane model: affirm the lanes, refine the count

**Affirm the distinctions; refine the arithmetic: two wire lanes and one cargo grammar, rather than three lanes.** Have-already and need-resin are genuinely different wire behaviors — refs only, versus bytes once. The vessel is different in kind: an amphora is *what crosses*, while the two lanes are *how each resin within it crosses*. Naming the vessel a third lane invites a third code path, and the sameness doctrine wants one. The refinement pays immediately in the frame design: **one frame shape serves all three cases.** A frame carrying a manifest slice and zero payloads *is* the have-already lane. The same frame carrying payloads for its misses *is* the need-resin lane. A sequence of such frames under one batch whose parent mark is the amphora's own *is* the vessel crossing. One shape, three uses — TigerBeetle's fixed-noun habit wearing Rye clothes.

The minimum frame preserving batching without breaking Tally gardens is given whole in I1. Its spine: a small **signed batch header** (control plane), a bounded **manifest slice** whose entries carry Tilak, algorithm-tagged digest, length, and name (control plane), and an optional **payload section** of resin bytes exactly as stored (data plane). The signature covers header and manifest only — **payloads prove themselves by digest**, because the digest already rides inside the signed manifest. That is the zero-copy dividend stated precisely: one signature verification per batch, one SHA3 walk per resin, and no second integrity pass, no re-encode, no owned intermediate buffer unless absorb truly requires one.

**Where toll attaches: to the fact, never to the resin.** The sorting in the ordering doctrine already answers this. Resin movement is owned-fact traffic — one hand names bytes, the other verifies them — so the have-already and need-resin lanes pay no ordering toll at all; they pay only the *egress budget*, which is a season constant at the tunnel mouth rather than a commons price. Toll lands **per batch** exactly when a batch carries facts that touch the commons — an availability claim published to a shared book, a MALA transfer riding as cargo — one signed header, one toll, one review. Per-peer-season is the accounting grain for the egress budget; per-vessel adds nothing the batch header does not already carry. Taxing resins would quietly turn synchronization into a commons expense, and the toll's whole economy rests on staying rare.

### B. Same resins, many Tilaks: keep the tree's answer; decline a seated `resin` Tilak

**Counsel: no seated `resin` Tilak.** A resin is an *addressing law* — how bytes are named, stored, and proven — while a Tilak is a *meaning mark* — what the bytes are. A content-addressed bytes marked `resin` would say "these bytes are bytes": a tautology mark that spends the seam's one label on the least informative fact about the cargo. The manifest line already carries the true mark per entry; the two hardcoded roots (`plain-bytes`, `manifest`) suffice for every resin whose meaning is still plain; and one-word-one-gate's spirit counsels against a second name for content-addressed bytes. Where the wire must say "this frame carries resin-law cargo," that is the **frame version field's** duty, never a Tilak's. Vision room, one sentence: if a future lap wants resins that carry their own layout promises — bead-series resins, checkpoint resins — those earn *specific* Tilaks (`bead-index`, `checkpoint`) at their laps, which is per-cargo marking working exactly as ratified.

**When Amber's ring-1 manifest graduates from `relpath + digest` to `<mark> <digest> <name>`: first, before any wire work.** The hygiene lap (I6) is the smallest metal stop in this whole arc and it unifies cellar and vessel under one line law before the Comlink frame exists — sameness is the macro, applied in time. Migration story in I6: additive, two goldens for one lap, old shape readable forever, new shape written from the lap forward.

### C. Sovereign snapshot: the smallest sovereign, the largest witnessable

**The smallest export that deserves the word *sovereign*:** the Kumara public trust bundle, the Weave slice to HEAD, Mantra's projection metadata, the resin closure those facts reference, and the signed manifest naming all of it — bundles A (public material only), B, D (referenced subset), and G. Sovereignty is *the ability to verify and reproduce state with no network and no third party*; that set already grants it, because a stranger holding only the export and the out-of-band fingerprint card can walk signature to root, replay the fold, and land on HEAD. The Brix closure makes the export *reconstructable*; the Aurora spine makes it *bootable*. Those are later, larger promises — worthy, and separate.

**Lap-two witness scope (in):** export writes the Weave slice, the Brix closure, and the superset manifest; import verifies the seal, maps resins read-only, replays the fold on a host, and asserts **HEAD equality** — plus the refusal paths: tampered resin, unknown Tilak, duplicated name, each refused whole. **Horizon (out, vision room until claimed):** Pond and Caravan policy bundles, the Aurora boot spine, checkpoint resins, incremental beading, warm WOV balances, and anything on bare metal. **The largest export that stays witnessable without thin-edge metalsmoke** is everything through lap three — the hosted Comlink crossing — because lap four's boot is the thin edge by definition, and a headless bench class can never claim it; that claim belongs to a hand at a machine, marked the metalsmoke way.

### D. Lap order: replay-on-host first, Puddle before USB, and the continuity fact at customs

**Replay-on-host before bare-metal boot — affirmed, and for a happy-zone reason, rather than merely a convenience.** The replay claim (fold equality) and the boot claim (world comes up) are different claims, and binding them in one witness is exactly the "whole desktop works" violation the claims paper names. Lap two proves the fold on a host. **Puddle's microVM then hosts the first boot-shaped replay** — lap four's first rung — because the microVM presses the boot claim as far into the happy zone as it can go while the world stays scriptable; Aurora USB on real metal follows as the thin edge's few honest checks, world-only, smallest slice, witnessed by a hand. This also gives Puddle its first lap for free: the ER's own line — one guest snapshot export, verify, restore — is the same rung.

**Double-boot law meets import as a customs check, enforced by Pond before placement.** The export carries a signed **continuity fact** (I7): identity, epoch, kind, parent, stamp. Import refuses whole when a live instance of the same Kumara identity runs on the receiving host, or when the export's epoch is stale against the last locally witnessed boot of that identity. The first boot after a successful import appends a fresh boot-kind continuity fact, so any second boot of the same export is detectably stale everywhere the log travels. One identity, one live world — ruled out by construction, exactly as the Puddle invariant recorded. One flag, safety-first: the continuity fact touches identity custody, so its exact shape is a **Kaeden gate**, parked in Part Seven rather than assumed here.

### E. Urbit parity: adopt the honesty, refuse the wire

**Adopt next, as concepts:** the **checkpoint resin** — pack's analog — enters the vocabulary now as *a resin whose Tilak names a folded-state view, derived and never authoritative*; its metal waits for lap five, and adopting the concept early costs nothing because a checkpoint is just another content-addressed unit under the existing law. The **practice-world** pattern — fake ships — maps to Puddle worlds with throwaway Kumara identities; its name stays parked in RTAC with the testnet candidates, per the naming budget. And **meld's deduplication arrives structurally free**: content addressing already stores one copy of identical bytes, so the analog needs no work at all, only this sentence.

**Permanently out of scope:** the jam/cue noun wire — we carry content-addressed resins and Bron views, never noun serialization; the persistent-heap image — our state is the log, never the heap; and any global PKI registry — Kumara plus petnames plus the settlement market answer identity in our own grain. The one Urbit sentence worth keeping whole is the one the tree already owns: *replay the log, and the world returns.*

### F. Priority: the Edit 5 slot, and eight weeks smallest-first

The designed-not-built witness reads **count=0** on this metal, so the slot is open, and the prompt's real question is which spec earns it. **Counsel: the snapshot export manifest spec (I2) takes the slot first.** It is the smallest spec that unblocks the most: the Amber hygiene lap (I6) rides its line law, the Comlink frame (I1) reuses its manifest section rather than inventing a second grammar, and its first lap — write and verify a superset manifest on a host — is witnessable with no new module and no gate beyond the word to draft it. The Comlink frame spec takes the slot **second**, after the snapshot spec's first lap runs green and frees it. The gloss spec is vocabulary canon rather than a functional spec, so it never occupies the slot and proceeds on its own word in parallel.

Eight weeks, smallest-first, gates honored and never pushed — vision room as to calendar, checkable as to order:

| Weeks | Stops | Gate |
|-------|-------|------|
| 1 | Kaeden words in one sitting where he chooses: TWO_ROOMS seat · gloss go-ahead · seed home · Edit-5 slot word · resin-Tilak word (B above) | Kaeden |
| 1–2 | Amber manifest hygiene lap (I6), red-then-green `[Cursor: verify on metal]` | mechanical |
| 2–3 | Snapshot export spec drafted into the slot; `snapshot_replay` witness red-then-green on host | Kaeden word, then mechanical |
| 3–4 | Amphora lap-two Tilak witness (eight-asks A6 design) the day the seed-home gate opens | Kaeden |
| 4–6 | Slot turns over: Comlink resin-batch frame spec; hosted have-already and need-resin witness on the localhost wire | word, then mechanical |
| 6–8 | Open Asks room design resumes per A4's ordering; SLC-L2 stays gated behind it; journey's pauses honored throughout | Kaeden |

**Where snapshot lap two ranks against the standing stops:** behind the one-sitting words (they cost minutes and unlock everything), beside the doorway witness (both wire the moment their words land), and ahead of SLC-L2 (per eight-asks A4, which this memo leaves undisturbed). The two tracks interleave rather than contend — preservation is Rye OS metal; Open Asks is Linengrow design — and the itinerary law orders each within its own lane.

### G. CIVIC alignment: portable sovereignty as the audit made whole

The sovereign snapshot is CIVIC's verification-from-outside promise made portable. The accountability layer's law — append-only, signed, checkable without trusting the application — is exactly what a sealed export delivers to an auditor's hands: a journalist, a skeptic, or a public-policy institute receives the amphora, walks every signature to the published root, re-folds the public ledger slice, and confirms the state the platform claimed, on their own metal, with the platform switched off. The separation between builders and auditors survives the crossing because the export's trust lives in resins and manifest, never in the exporting software. Issuer transparency gains a standing artifact: the giving ledger sealed at a stamp is an audit object anyone can replay. And the reward stays pointed at the good: what the snapshot measures — verifiable receipt of what communities actually received — is the outcome itself, carried whole.

The boundary, stated as one sentence of law for Kaeden's ratification: **a civic-facing amphora carries only facts already published to the commons, plus the proofs required to verify them; everything else stays home.** Named on the never-list: warm private keys in any form (already absolute law); private care facts about sanctuary residents without per-fact consent marks — the dignity floor extends to data, and consent-before-counting governs export exactly as it governs tallying; unpublished personal data of third parties; the labeled *content* behind moderation receipts — the label facts travel, because they are governance's receipts, while the bytes they point at stay refusable at every store; and host telemetry fine enough to fingerprint a home. Aggregates that serve the dignity-floor accountability travel as **derived, consented projections**, never as raw resident facts. Surveillance is the failure mode where the export outgrows the commons; the one-sentence boundary is the wall.

### H. The tunnel mouth: have-already as the civic default — affirmed, with one refinement

**Affirm: the have-already lane is the civic default for Comlink resin sync across the front door**, and the three standing facts compose into one story without a new mechanism. The egress budget is a named season constant metered at the tunnel's mouth; the default sync sends refs, so a month of vigorous civic life costs megabytes; bytes cross only on genuine miss, and each miss debits the budget; the household headroom fraction is never crossed; and a store's decline is a signed **resin-refusal fact** — the no gets its receipt without describing the refused. Toll stays out of it entirely, per A: resins are owned-fact cargo, and the front door's constraint is budget, never ordering.

One refinement for the refusal receipt: carry the **reason class** as a small enum — *budget* (out of season allowance), *policy* (this store declines this address), *bounds* (resin exceeds a named maximum) — because the three mean different things to an auditor and to a neighbor deciding where to fetch next. All three stay content-silent; all three leave receipts; and the budget class quietly teaches peers to route popularity away from home, which is the paper's own deepest answer arriving as an incentive rather than a rule.

---

## Part Two — Implementation Architecture (Spec Grain)

Designs Cursor can draft into `context/specs/` or `active-designing/` on Kaeden's word. No code here; every constant below is a **counsel default with rationale**, offered for pinning rather than claimed as measured.

### I1 — Comlink resin-batch frame

One frame shape for all three uses (A above). Signature covers header plus manifest; payloads prove themselves by digest.

| Field | Type / bound | Why |
|-------|--------------|-----|
| `frame_version` | `u64` chronological stamp | Versioning rule: authored surfaces carry chrono stamps; eras verify side by side |
| `sender_key` | 32 bytes, Ed25519 public | Explicit identity at lap one; key-id compression is a later, measured lap |
| `parent_mark` | 32 bytes, SHA3-256; all-zero at genesis | Amphora's fourth vow made a field: every batch names its parent |
| `batch_stamp` | `u64`, sender-supplied, signed | Advisory ordering within a peer's own story; never Kaeden's clock, never authority |
| `manifest_line_count` | `u32` ≤ `max_manifest_lines` | Bounds on everything; review stays bounded |
| `payload_count` | `u32` ≤ `manifest_line_count` | Misses only; hits are never re-sent |
| `total_payload_bytes` | `u64` ≤ `max_batch_payload_bytes` | Pre-check before any allocation; Tally garden sized by this |
| `signature` | 64 bytes, Ed25519 over header ‖ manifest | One verification per batch — the control plane's whole cost |

Manifest entry, the wire's superset of the seated three-field line:

| Field | Type / bound | Why |
|-------|--------------|-----|
| `tilak` | bounded string ≤ `max_tilak_bytes` | Per-cargo meaning mark; unknown mark refuses the batch whole |
| `algo` | `u8` enum: 1 = SHA3-256 · 2 = SHA3-512 | Resins law: tag when tiers mix; eras coexist on one wire |
| `digest` | 32 or 64 bytes per `algo` | The address and the proof |
| `length` | `u64` ≤ `max_resin_bytes` | Receiver bounds-checks before reading a single payload byte |
| `name` | bounded ≤ `max_name_bytes`, unique in batch | Duplicate name refuses whole (eight-asks A6 law, promoted to the wire) |

Payload section: resins concatenated in manifest order for misses only, **bytes exactly as stored** — the digest names what is stored, so nothing re-encodes on send. Refusal and error codes, named for the fault: `error.BadSignature` · `error.StaleParent` · `error.UnknownTilak` · `error.DuplicateName` · `error.DigestMismatch` · `error.LengthMismatch` · `error.OverBudget` (reason class *budget*) · `error.RefusedResin` (reason class *policy*, references a signed refusal fact) · `error.OverBounds` (reason class *bounds*). Every refusal is whole; partial trust of a batch does not exist.

### I2 — Snapshot export manifest

**No new grammar.** The snapshot manifest is an amphora manifest: flat three-field lines (wire superset adds `algo` and `length` as in I1), with **bundles expressed as name prefixes** — `trust/…`, `weave/…`, `mantra/…`, `brix/…`, `resins/…`, `policy/…` (horizon), `boot/…` (horizon), `catalog/…`. The flat law scales by naming, the seed parser already witnessed at 151 keeps working, and the self-describing drive law is satisfied by entries rather than by format: the catalog copy, the `PUBKEYS.md` slice, the master public key, the sandbox certification, and any revocation certificates all ride as ordinary named resins under `trust/` and `catalog/`.

One distinguished first entry: the **head record** — a `manifest`-marked resin containing the snapshot's own claim, signed: HEAD digest at the **SHA3-512 canonical tier** (a forever-name, per the tiers law), export stamp, identity epoch, the continuity fact (I7), and the export-time parity claim as a checkable sentence (count plus bench, cited rather than trusted). Import's first act is verifying this record; everything else follows from it.

### I3 — Import rite state machine

| State | Does | Wreck exit |
|-------|------|-----------|
| `RECEIVE` | Vessel lands whole | media error → refuse |
| `VERIFY_SEAL` | Signature to root; parent mark; algo tags known | `BadSignature` · `StaleParent` |
| `VERIFY_MANIFEST` | Every line: known Tilak, unique name, length ≤ bound | `UnknownTilak` · `DuplicateName` · `OverBounds` |
| `MAP_RO` | Resins mapped read-only (mmap where host allows; plain bounded reads otherwise — named honestly per host) | host error → refuse |
| `VERIFY_RESINS` | Batched SHA3 walk; every digest equal | `DigestMismatch` |
| `CONTINUITY` | No live same-identity instance; epoch fresh | `ContinuityViolation` |
| `REPLAY` | Pure fold of Weave facts; **assert HEAD equality** | `FoldMismatch` |
| `SHAPE_CAST` | Brix closure cast; every brick's digest present | `MissingBrick` |
| `CUSTOMS` | Pond policy applied before placement | policy refusal, with receipt |
| `PLACED` | Season garden promoted atomically | — |

Nothing is visible outside the season garden until `PLACED`; a wreck at any state leaves the host exactly as found. Fails whole — the vessel's third vow, kept at every seam.

### I4 — Witness ladder (laps one through five)

| Lap | Witness | Claim it binds | Parity |
|-----|---------|----------------|--------|
| 1 | `tools/amber_first_ring.rish` | Export, verify against independent oracle, restore; tamper fails | **144** ✅ existing pin |
| 1 | `tools/crossing_manifest_seed.rish` | Three-field line parses | **151** ✅ existing pin |
| 1½ | `amber_manifest_tilak` (I6) | Ring-1 manifest speaks the three-field law; both goldens; tamper and unknown-mark refuse | `[Cursor: verify on metal]` |
| 2 | `snapshot_replay` | Export at HEAD; import on host replays to **equal HEAD**; tampered resin, unknown Tilak, duplicate name each refuse whole | `[Cursor: verify on metal]` |
| 3 | `amphora_comlink_resin_batch` | Hosted wire: hits cross as refs only; misses cross once; second sync of same batch moves zero payload bytes | `[Cursor: verify on metal]` |
| 4 | `aurora_snapshot_boot` | Boot-shaped replay — **thin edge**; first rung inside a Puddle microVM, final rung a hand at real metal, metalsmoke-marked | never claimed by a headless bench |
| 5 | `snapshot_incremental` | Rolling-bead export; small edit reseals a small region | horizon; waits for a measured red |

Red-then-green order within each lap, per standing law; every wire into `tools/parity.rish` lands with its own count verified on the wiring bench, never inherited from this table.

### I5 — Tally bounds (counsel defaults, offered for pinning)

| Constant | Counsel default | Rationale |
|----------|-----------------|-----------|
| `max_manifest_lines` | 65 536 | `u32` count; one review sitting; a snapshot larger than this names a bead-index resin instead |
| `max_tilak_bytes` | 64 | A mark is a word, never a sentence |
| `max_name_bytes` | 4 096 | Path-shaped; matches host path ceilings |
| `max_resin_bytes` | 1 GiB | One sequential read per resin; larger objects arrive as bead series at lap 5 |
| `max_bead_bytes` | 256 KiB | Lap 5 only; upper edge of the studied content-defined range in the Puddle second pass |
| `max_batch_payload_bytes` | 16 MiB | Hosted lap: one bounded Tally garden holds one batch whole |
| `max_vessel_bytes` | season constant | The egress budget's own child; per-crossing, per the tunnel-mouth story in H |
| `max_replay_garden_bytes` | named at import | The fold streams; the garden bounds it; a snapshot demanding more names it in its head record |

Each lands in code as a named constant with its comment, per the root discipline; each is Kaeden's or Cursor's to re-pin at the lap that first enforces it.

### I6 — Amber manifest hygiene lap

Delta from ring-1's `relpath + digest` to the seated `<mark> <sha3-256-hex> <name>` line, additive at every step. Amber's writer emits the three-field line with mark `plain-bytes`; a second golden fixture carrying the new shape lands beside the existing golden, and the witness asserts **both** parse paths for one lap — the elder golden preserved, accrete-never-break, so the shape's history stays checkable. The reader accepts both shapes permanently; the writer emits only the new shape from this lap forward. Tamper and unknown-mark refusals carry over from the seed witness's law. One lap, one commit family, first metal stop of the arc — and from it forward, cellar manifests and vessel manifests are one grammar, which is the sameness the whole wire design then inherits.

### I7 — Continuity / double-boot fact

Bron shape sketch, offered for the custody gate rather than assumed:

```
continuity {
  identity  <kumara-public-key>
  epoch     <u64, strictly increasing per identity>
  kind      export | boot
  parent    <sha3-256 of prior continuity fact; zero at genesis>
  stamp     <supplied by the person, one clock>
  signature <sandbox key over the whole>
}
```

Pond's refusal rule, two sentences: an import is refused while a live instance of the same identity runs on the receiving host; an import or boot is refused when its epoch is less than or equal to the last boot epoch this host has witnessed for that identity. The fact touches identity custody, so its final shape, its epoch source, and its interaction with key rotation are **Kaeden's gate** — parked, never improvised.

---

## Part Three — ROADMAP Granular Refresh (structure and deltas only)

**1 · Current position paragraph** — replace the doors-and-ladder framing (all boxes closed) with one standing paragraph: *Parity 152 on two benches from bare; SLC-1, SLC-2a, SLC-2b, RW-1 through RW-4 landed; SLC-L1 landed and affirmed closed at lap one; the vocabulary cluster (gate · witness · toll · itinerary · lap · Tilak · Pottery · Amphora · cellar/vessel) seated; zip 41 counsel landed and zip 42 counsel in hand; the Edit 5 slot open and awaiting its next spec.* Exact sentence at Cursor's phrasing; the pins are the content.

**2 · Ground table** — add three rows: **Amber** (cellar software; ring-1 export/verify/restore; witness at **144**), **Amphora** (vessel software; manifest line law; seed parse at **151**; packet formalization in flight), **Linengrow receipt** (sign → append → fold → verify; witness at **152**). One honesty edit in place: Tablecloth's row already says *designed (future)*; keep it, and give Puddle no row until its first lap runs.

**3 · Preservation and portability** — counsel **a named subsection inside Horizon 1**, titled *Preservation and portability — cellar, vessel, snapshot*, rather than a new numbered horizon. Reasoning: the numbered horizons are directions of travel, and preservation is already on the primary track's ground at 144 and 151 — a "Horizon 1½" would announce a new track where the tree has, in truth, a new *rung*. The subsection lists the lap ladder from I4 with its gates.

**4 · Linengrow track** — SLC-L1 row gains *closed at lap one (eight-asks A4)*; SLC-L2 row gains *gated behind Open Asks and Kaeden's order*; a new one-line row under the horizon: *civic export boundary (counsel G) awaits ratification before any civic-facing amphora.*

**5 · Version line** — bump `Version:` to the anchor of the applying commit with slug *preservation-arc pass*; `Last updated:` same date; prior editions endure in history, per the file's own law.

**6 · Trio line** — the 152 sentence already names the suite through SLC-L1; append the *projected* witnesses by name only — amber manifest tilak, snapshot replay, doorway (when TWO_ROOMS seats), amphora comlink resin batch — with no counts attached, per pin-what-the-bench-reports.

---

## Part Four — TASKS Granular Refresh

**1 · New section** — *Sovereign snapshot and zero-copy resins (`20260704.181012`)*, rows with owners and gates:

| Row | Owner | Gate |
|-----|-------|------|
| Edit-5 slot word: snapshot export spec first, Comlink frame second | Kaeden | word |
| Resin-Tilak ruling (counsel B: decline; per-cargo marks stand) | Kaeden | word |
| Amber manifest hygiene lap (I6) | Cursor | mechanical; parity `[verify on metal]` |
| Snapshot export spec draft (I2) into `context/specs/` | Cursor | after slot word |
| `snapshot_replay` witness, red-then-green (I3, I4 lap 2) | Cursor | after spec's first lap |
| Continuity-fact shape (I7) | Kaeden | custody gate |
| Comlink resin-batch frame spec (I1) | Cursor | after slot turnover |
| Hosted resin-batch witness (I4 lap 3) | Cursor | after frame spec |
| Civic export boundary ratification (G) | Kaeden | word |
| Refusal reason classes (H) accreted into the resin-refusal row | Cursor | doc delta |

**2 · Sweep candidates** — the Three Doors block and ladder items 1–6 (every box checked), the SLC-L1 scope-note and build blocks, and the RW-1/RW-2 rows: sweep each to a session-log archive stub and prune, per the file's own edited-in-place law. The **recorded rulings** paragraphs stay — they are standing law reference, never task residue.

**3 · Duplicate strikes** — one: *Comlink device wire* appears both at the Now-ladder's tail and under Soon; keep the Soon row, strike the tail line (*why:* one home per stop). The Puddle pair (exploration horizon; continuity invariant) are distinct claims and both stand.

**4 · Crossing suite follow-ups** — restate as rows: Amphora lap-two Tilak witness (eight-asks A6) — owner Cursor, gate seed-home; seed module home — owner Kaeden; manifest module home rides the same word. Dependencies: the lap-two witness depends on the seed home; the snapshot spec depends on neither and proceeds first.

**5 · Open horizons block** — tie each standing row to this arc in one clause: *movement's open asks* → unchanged, ordered before SLC-L2 (A4); *Puddle continuity* → shape now sketched at I7, gate custody; *deep-archive hardware* → the sealed-archive lap's substrate; unchanged, in season; *resin-refusal fact* → gains the three reason classes from H.

**6 · Open-threads stub** — keep archived; no resurrection. Any thread still alive already has a decoder row or gains one below; the decoder is the living home for standing questions, and one home per question is the same law as one home per word.

---

## Part Five — Decoder Rows and the RTAC Itinerary

New rows for the decoder's three-column table:

| The standing question | What it truly asks | What answering unlocks |
|----|----|----|
| **Sovereign snapshot (lap 2)** | Can a Weave slice plus Brix closure exported at HEAD replay on a second host to the identical HEAD, with no network? | The preservation pair's first portable proof; the 3-2-1 rule's offsite copy becomes a witnessed fact |
| **Zero-copy resin batch** | Can many resins cross in one signed frame where hits cost refs only and payload bytes never re-encode? | Comlink v1's cargo shape; packaging's fetch path; the egress budget kept honest by construction |
| **Resin Tilak** | Does a resin need a mark of its own, or is the mark always the cargo's? | The manifest line law finalizes; the frame spec unblocks (counsel B answers *the cargo's*; the row stands until Kaeden's word) |
| **Snapshot replay witness** | What is the smallest fold-equality claim one witness can bind on host metal? | Lap 2 red-then-green; the Edit 5 slot turns over cleanly |
| **Civic export boundary** | Which facts may leave home in a civic-facing amphora, and under whose consent? | Linengrow audit exports; the dignity floor extended to data; surveillance ruled out at the wall |

**RTAC open itinerary, proposed stops (smallest-first), Kaeden's gates listed above them untouched:** first the one-sitting words — TWO_ROOMS seat, gloss go-ahead, seed home, Edit-5 slot, resin-Tilak, continuity custody, civic boundary — each a minute of speech unlocking a staged week; then the Amber manifest hygiene lap, mechanical today; then the snapshot export spec into the freed slot and its replay witness red-then-green; then the seed-home-gated Amphora lap-two Tilak witness the day that word lands; then the frame spec at slot turnover and the hosted batch witness; and throughout, journey's pause honored the moment the itinerary empties. No standing gate is canceled, reordered, or pushed by this paragraph.

---

## Part Six — TAME and TigerBeetle Habits, Answered Explicitly

**Safety first** holds at every seam this counsel touched: the wreck rule refuses whole at vessel grain, unknown Tilaks refuse whole at type grain, every field in I1 carries a named bound, the continuity fact rules out double-boot by construction, warm keys never ride any export medium, and the civic boundary keeps consent in front of every crossing that touches a person. **Performance second** arrives as batching (one signature, one SHA3 walk, one garden per batch), the have-already lane (the second crossing of anything costs refs), and the drive seam named honestly per host — mmap read-only where the host allows, plain bounded reads otherwise, sendfile-class paths a named seam rather than a requirement. **Joy third** is the lap shape itself: lap two's promise — *export your world, replay it elsewhere, watch the same HEAD return* — is lovable at its own scale on the day it greens, no heroic ISO required. The studied habits map cleanly: control plane and data plane are the signed header-plus-manifest against the payload bytes; in-place initialization governs the frame structs when they are authored; payloads pass as `*const` slices with no alias games and no accidental stack copies; and Zig 0.16's explicit `io` threads the read-and-send path with no hidden globals. **Edit 5:** at most one functional spec in flight — the slot stands at count zero on this metal, and counsel F names the snapshot export manifest spec as the one that earns it, with the frame spec queued behind its first green.

---

## Part Seven — Risks and Gates

**Kaeden's gates, listed and never pushed:** TWO_ROOMS seat · gloss spec go-ahead · crossing-seed module home · doorway witness word · Edit-5 slot assignment (F) · resin-Tilak ruling (B) · continuity-fact custody shape (D, I7) · civic export boundary (G) · snapshot-law seating when the spec drafts · journey's pause, always his. **Mechanical for Cursor on those words:** the I6 hygiene lap; drafting I2 then I1 into specs; wiring each witness red-then-green with counts verified on Cursor's own metal; applying the Part Three, Four, and Five deltas as staged hygiene commits; README rows and session logs per standing discipline.

**Risks, named:** drafting the frame spec before the manifest law would mint two grammars for one line — mitigated by the I2-before-I1 order; beading before a measured red is machinery without work — lap five waits, per the ordering doctrine's own patience; a headless bench can never claim lap four, and any summary that forgets this trades away the metal-path honesty — the thin edge stays a hand's; attaching toll to resins would tax the commons into every sync — the sorting in A is the wall; and the civic export is the one place a good tool could quietly become a surveillance bundle — the one-sentence boundary plus consent marks stand in front of it, and nothing civic crosses before Kaeden ratifies the wall.

---

## Session Log Stub (for this packet)

`session-logs/archive/20260704/20260704-181612_zip42-152-zero-copy-counsel.md` · Editor: Claude (claude.ai bench) · Model: Claude Fable 5 · Voice: awaiting Kaeden's word · Stamp: `20260704.181612 UDT` · Prompt: zip 42 raised and pinned at 152 from bare; four readings in order; counsel A–H and I1–I7 delivered as this proposed ER; Cursor-lane response composed; every gate left with Kaeden.

---

## Gratitude

**TigerBeetle** (Apache-2.0) — batching, control and data plane, in-place initialization, and the zero-accidental-copy habits studied in `gratitude/TIGER_STYLE.md`. **Urbit** (MIT) — pier, replay, pack and meld, and the double-boot hazard, studied in `gratitude/urbit/`. **systemd casync and its faster sibling** — the chunk-store lineage and the tar critique carried through the Puddle second pass. **Jason Cohen** — the SLC frame every lap here wears. Concepts entered through the clean room; no outside word crossed into Rye's surface.

---

*May one frame shape carry three duties, and the second crossing of anything cost a name alone. May the smallest sovereign export prove itself to a stranger with the platform switched off. And may every gate stay Kaeden's, every count stay the bench's, and every resin stay exactly the bytes it claims.*
