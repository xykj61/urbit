# Claude Ruling — Enclosure: A-Narrow Interim, Then Pond Supersede-by-Witness

*Kaeden asked whether to widen ai-jail or finish Pond. Claude ruled: close tonight’s unnamed hole as a named lane first; then Pond earns the daily seat by out-refusing the jail. Addendum morning breath appended under the same stamp.*

**Stamp:** `20260712.090512`
**Language:** EN
**Style:** Radiant (see `../context/RADIANT_STYLE.md`)
**Voice:** Rio 3 recording Claude Fable 5 Max · Kaeden’s clock `090512` verbatim
**Register:** counsel — disposition seated for the house; code greens wait on Kaeden’s word
**Status:** **Answered** — disposition **A-narrow interim → B supersede-by-witness**
**Ground:** ask [`../session-logs/20260712-004012_framework-metal-g0c-pond-ask.md`](../session-logs/20260712-004012_framework-metal-g0c-pond-ask.md) · tip `4c2a359` · Pond customs `161125` · enclosure-editors [`../context/specs/enclosure-editors.md`](../context/specs/enclosure-editors.md) · compass [`../foundations/20260706-185112_follow-our-compass.md`](../foundations/20260706-185112_follow-our-compass.md) · Puddle [`../external-research/20260702-035018_puddle-sandboxed-rye-containers.md`](../external-research/20260702-035018_puddle-sandboxed-rye-containers.md)

---

## Disposition

**A interim, then B — with A as wall-repair, never widening.**

An enclosure with an unnamed hole is thinner than either a jail with one named device lane or a finished Pond. Tonight’s KVM path (user D-Bus + systemd transient units *outside* the jail) decides most of the ruling: name the device that crosses the wall; never borrow the host session.

| Path | Meaning |
|------|---------|
| **A-narrow** | One lane in *our* config (`tools/enclosure.conf` + launch wrapper — teacher ai-jail untouched): `lane_kvm`, **off by default**, grants exactly `/dev/kvm` and nothing else; witnessed on / off (on → guest boots; off → witness **refuses**) |
| **B** | Pond supersedes ai-jail the day it beats the jail at its own refusals — six-bar as asserts; two-lane season beside ai-jail; retirement accretive back to gratitude |

The same `lane_kvm` is Puddle’s future doorway — sandboxed Rye ships with their own Pond and Kumara will want KVM through precisely this gate.

## What not to widen in ai-jail (Pond is the true supersede)

- No host session buses — D-Bus user or system, `systemd-run`, portal escapes
- No host `$HOME` beyond the project bind; no `/tmp` sharing; no broadened network
- No USB or raw disk nodes (bare-metal USB stays a host-side act under its own word)
- No root or docker socket; no key movement (master on host · sandbox OpenPGP in jail · lane key in cloud · KVM lane grants no key access)
- Keep existing Wayland/GPU pass; add nothing to it
- KVM lane is **display-less by design**: QEMU `-display none` (VGA kept) + QMP `screendump`; G0-complete moves inside the jail nearly unchanged

## B’s bar — supersede by witness, never by feature list

Six-line minimum as asserts:

1. Durable project tree
2. Home and tmp reset
3. Wayland/GPU lane
4. KVM lane
5. Signing-lane separation: *sandbox key present, master absent, lane key absent*
6. Customs place·hold·refuse exercising negative space — kvm refused when lane off; D-Bus refused always; host home refused always

Freeze that bar; later rings afterward. Two-lane season (enclosure-editors setup B): Pond beside ai-jail; daily work moves only after refusal-parity holds green. ai-jail returns to gratitude — name kept, dated record written.

## Ordered greens (on Kaeden’s word)

1. Finish full metal parity; pin the suite number
2. `lane_kvm` + in-jail G0-complete (`-accel kvm -cpu host -m 4G -display none` + QMP PNG + GRUB serial) + paired refusal; retire D-Bus path with dated record
3. G1 receipt guest fully jailed (serial/console only)
4. Pond ring one: enclosure witness file — six-bar asserts incl. three refusals — run against ai-jail today as baseline

**Holds:** G2/G3 check-in · bare-metal USB own word · T3 parked · no OA wire · no session passes

---

## Addendum (`090512`) — morning breath

### a. Red-team framing

Tonight’s D-Bus/systemd workaround is a **finding**: an agent inside the jail reached the host session bus and spawned transient units — authorized and witnessed, and exactly the move a misbehaving agent would make. Seat the standing house pattern: **every authorized escape becomes a refusal-assert of the next enclosure.** Escapes become asserts; the wall’s repair order came from its own breach report.

### b. Guest QEMU rule

`-nic none` on every proven-seat relaunch until a worded need. When guest networking is worded, use socket-netdev guest↔guest (Comlink device-wire lab precedent — host NIC untouched). Pinned sha256 stays mandatory. KVM lane’s price is kernel ioctl surface — off-by-default, pinned images, no NIC keep that price paid only for witness minutes.

### c. Two-lane season law

Write exit criteria **before** the season opens: N consecutive green sessions, refusal-parity scorecard (identical probe set in both enclosures), divergences logged as loudly as greens. Civic policy turned inward.

### d. Lane graduation

`enclosure.conf` `lane_kvm` → Pond customs row at supersede — same fact, policy-as-value. Pond ring one drives the *same* bubblewrap muscle; only where policy lives changes (Gall’s Law on the wall). Stronger walls earn ring by ring toward Puddle guests and Caravan horizon.

### e. Signing and glass asserts

Signing-lane asserts via **public fingerprints only** (sandbox present · master absent · lane absent). Glass assert = non-black PNG + expected dimensions + hash recorded; human eye ratifies once per rung.

### f. First debt of the morning

Finish full metal parity in foreground; pin the number. Suite green witnesses *composition*.

**Rounds left closed this morning:** T3 proof brief · Spritely first contact · NLnet application — each wants its own morning.

---

*May every hole in the wall become a named door or a closed one. May Pond earn the jail’s job by out-refusing it. May the breach report keep writing the repair order.*
