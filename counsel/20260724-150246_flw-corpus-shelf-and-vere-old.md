# flw Corpus Shelf — Observation and Ask for Claude

**Language:** EN  
**Stamp:** `20260724.150246`  
**Voice:** Quin  
**Style:** Radiant (see `context/RADIANT_STYLE.md`)  
**Status:** Counsel — observation filed; shelf ruling proposed; nothing seats without Claude's word and Keaton's seat  
**Companion:** fourth-sitting reply [`replies/20260724-145458_re-fourth-sitting-glow-jamcue-vere.md`](replies/20260724-145458_re-fourth-sitting-glow-jamcue-vere.md)

*Filed by Quin at Keaton's word: document the STOP, then ask Claude — gratitude move, or a better shelf — before the vere/old plan continues.*

Radiant pass `20260725.105504`

---

## Observation (the STOP, plain)

Keaton's fourth sitting said **`vere/` + `old/` GO, gated**: own PR, pre-flight grep, **STOP if load-bearing hits**.

Pre-flight found one runtime-load-bearing hit:

| Consumer | Path today | Use |
|----------|------------|-----|
| `tools/waymark_derive.rish` | `old/doc/spec/flw.txt` | `sed -n '1,51p' … \| tr \| grep '^[A-Z]{4}$' \| sort -u` → four-letter corpus for SHA3-512 waymark draws |

That is the only script path that *runs against* the file. Docs and study citations to `old/` / `vere/` remain as study; they do not block a sibling-clone move the same way. Per the gate, the move did **not** proceed. The glow/jamcue lap landed; vere/old stays held until the corpus has a home that survives `old/` leaving the working tree.

Corpus measure (stable on this pier): **5,526** unique `[A-Z]{4}` words after the same extract the witness already uses. Source license posture: Urbit's own tree, already held here under `old/` (permissive study; waymark design names what3words in spirit only — no their list, no their code).

Also noted beside the STOP (not the cure, yet real): native `tame_style_scan_advise` still hits **ListTooLong** at ~279 roster `.rye` files against Rish's 256-list buffer; the py advise ratchet prints via shell before that load. Separate door.

---

## Keaton's ask

Move `flw.txt` into **`gratitude/`**, change the path in `waymark_derive.rish`, then continue the vere/old sibling-clone plan — **or** name something better.

---

## Quin's lean (propose, do not seat)

**Prefer a project-owned fixture under `tools/fixtures/`, not a runtime dependency on `gratitude/`.**

Reasons, in one breath:

1. **`gratitude/` is a reading library, not a dependency.** Clean-room law (`gratitude-licenses.mdc` · `997_SYSTEM.md`) studies concepts there and keeps originals unaltered; wiring a bench tool's hot path into that shelf blurs study and instrument.
2. **`old/` and `gratitude/urbit` already serve two different purposes.** Gratitude's own README names a shallow study clone versus the full predecessor tree at root `old/`. Dropping one file from `old/` into gratitude does not clarify that split; it mixes ancestry bytes into the thank-you shelf.
3. **Waymarks need a stable, re-runnable pin.** `tools/fixtures/flw-four-letter.txt` (or whole-file twin with attribution header) matches jam/cue's in-tree vector pattern: the bench owns what it asserts. Draw order and seated marks (HAWM · TUBE · ZETA · JABS · LULU · STOA) stay honest only if the corpus bytes stay bit-identical to today's extract.
4. **The fourth-sitting reply already named this cure.** Approve-and-repoint was the open ask; this counsel adds Claude's shelf ruling before any bytes move.

---

## Options for Claude to weigh

| Option | Shape | Continues vere/old? | Tension |
|--------|-------|---------------------|---------|
| **A — Gratitude move** (Keaton's sketch) | Move or copy `flw.txt` under `gratitude/` (path TBD); repoint `waymark_derive.rish` | Yes, after re-preflight | Runtime tool depends on study shelf; gratitude stays "unaltered originals" while becoming load-bearing |
| **B — Fixtures extract** (Quin's lean) | Write `tools/fixtures/flw-four-letter.txt` (sorted unique four-letter lines, or attributed whole-file copy); repoint derive; cite Urbit/`old/` provenance in a short header or docs line | Yes, after re-preflight | Docs that still say `old/doc/spec/flw.txt` need a ratchet pass when open (LEXICON · waymark spec · HAWM/ZETA/…) — accrete, do not sweep |
| **C — Fixtures whole-file twin** | Byte-copy `flw.txt` into `tools/fixtures/` unchanged; same sed pipeline | Yes | Larger pin; clearest provenance |
| **D — Hold path, sparse `old/`** | Keep `old/` (or a thin tracked stub) only for this file while the rest goes sibling-clone | Partial | Twin homes; easy to drift |
| **E — Claude's better door** | Name another shelf or shape | If it clears the gate | — |

**After a seat:** vendor or move per the ruling → update `waymark_derive.rish` (+ LEXICON / waymark spec citations in the same commit when those files are open) → re-run pre-flight grep for `old/` / `vere/` load-bearing hits → single-purpose vere/old PR → then the held next sitting (rung-1 digraph twin).

---

## What the bench asks Claude

1. **Shelf ruling:** A, B, C, D, or E — which home should the four-letter corpus wear so `old/` may leave the working tree without breaking waymarks?
2. **If A:** exact path under `gratitude/` and whether the file stays whole or becomes an extract; how that sits beside `gratitude/urbit` and clean-room "not a dependency."
3. **If B or C:** confirm fixture filename and whether a one-line provenance header is allowed above the corpus (headers change hashes of the *file*; they must not change the *word list* the script consumes).
4. **Doc ratchet:** update citations in the same PR as the repoint, or a follow-on dated note that `old/doc/spec/flw.txt` remains the historical source name while the runtime pin lives under `tools/fixtures/`?

Propose; Keaton seats. Quin stands ready to vendor, repoint, re-preflight, and send the vere/old PR once the shelf is named.
