# 997 · Open Threads — As of 2026-06-18, Afternoon

*A fresh snapshot, superseding `998`, taken as we pause to run a radiant pass over all our writings. It records what has closed since the last gathering and what still waits, so the next session begins exactly where this one rests.*

**Language:** EN
**Version:** `20260618.171512` (Rye chronological stamp)
**Last updated:** 2026-06-18
**Style:** Radiant (see `../context/RADIANT_STYLE.md`)
**Voice:** Reya 2
**Status:** Snapshot (the newest open-threads gathering; supersedes `998`)

---

## Closed Since the Last Snapshot

- **Rishi is named and settled** — the seer's shell, with `.rish` scripts — and carried consistently across `988`, `989`, and `996` (the Brook placeholder retired everywhere).
- **The negative self-test landed** — `tools/parity-selftest.sh` proves the parity gate turns red on a real divergence, so its green now means something.
- **Four writings grew the picture** — `990` (shells through the TAME lens), `989` (the voices of an OS), `988` (Rishi, and the parity gate three ways), and `996` (how we grow a language).
- **A radiant pass** smoothed the Rishi cluster, and is being extended across every one of our `.md` writings as we pause.

---

## Still Open — Awaiting a Decision

- **The strategic fork — resolved.** Rye is its own language, aiming to surpass Zig along the axes of TAME and RISC-V; recorded in `../context/specs/rye-as-its-own-language.md`. This unblocks the first Rishi and the promotion of the `989` family into `active-designing`.
- **The orchestration language's name** — the execline-spirit member of the family is still unnamed.
- **`9999_STRENGTHENING` vs `9999_TAME_STRONG`** — a naming choice Reya made provisionally, still open to your word.

---

## Still Open — Roadmap

- **The additive-only gate (#1)** — prove a strengthening pass changes only assertions, `maybe`, and comments.
- **A minimal Rishi** — an interpreter in Rye, scoped to run exactly `parity.rish` (gated on the strategic fork).
- **Vendor execline** — the near-term TAME bridge for the small build scripts.
- **A minimal Mantra weave** — overlay, always-succeeding merge, and line-provenance; the de-risker of baseline drift.
- **Promote the `989` family into `active-designing`** — once the fork is decided.

---

## Housekeeping

- **The `.gh` token** — keep it for future forge operations, or remove it and revoke the personal access token.

---

## Resolved, No Action Needed

- **The Verified badge** — every commit shows green on both forges, signed by the sandbox key.

---

*May this resting place be honest, and the next beginning clear. May what we closed stay closed, and what waits stay visible. And may we return gently, to find the work right where we left it — calm, and ready.*
