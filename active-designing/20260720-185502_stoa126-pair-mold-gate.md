# STOA126 — Pair-Mold Gate Sample (Multi-Field `$:` Identity)

*An Acme Corporation employee reading this gets the thinnest honest multi-field `$:` gate-sample deepen after STOA125: `sample=pair-mold` on bartis and barket lowers as a closed `PairMold` struct identity with baked from 5 / amount 3, without ambient gold, argv, call-body, or Sala B0.*

**Language:** EN  
**Version:** `20260720.185502` (UTC · cloud host)  
**Style:** Radiant (see `../context/RADIANT_STYLE.md`)  
**Voice:** Quin  
**Status:** Checkable — nest · bartis · barket witnesses bind pair-mold accept  
**Register:** Checkable — `glow_nest_type_witness` · `glow_lower_bartis_witness` · `glow_barket_lower_witness` 
**Grounds in:** [`20260720-184953_stoa125-xfer-mold-gate.md`](20260720-184953_stoa125-xfer-mold-gate.md) · [`../glow/gen/mold-pair.glow`](../glow/gen/mold-pair.glow)

**dated_guard:** Do **not** edit the dated STOA125 wording artifact. Seat this lap in *this* brief and living pins when GREEN.

---

## Verdict (one breath)

**PAIR_MOLD_GATE.** Widen the closed gate-sample allowlist with **`pair-mold`**. Parse accepts `|=` / `|^` `sample=pair-mold` identity. Lower emits `PairMold = struct { from: u32, amount: u32 }` and a welcome main baked to `.{ .from = 5, .amount = 3 }`. Call-body and argv under pair-mold stay refused. Open multi-field `$:` beyond pair (`triple-mold`) stays refuse. Ambient gold · Sala B0 held.

---

## Why this is the honest next lean A

STOA125 closed the multi-face `$%` ladder. Remaining doors were gold vs multi-field `$:` vs call-body under tagged molds. `pair-mold` already has a GREEN mold lower (`struct`); wiring it as a gate sample is thinner than ambient gold and thinner than union call-body demos.

---

## What this lap lands (when GREEN)

| Item | Proof |
|------|-------|
| Allowlist includes `pair-mold` | nest_type |
| `gate-pair.glow` · `gate-barket-pair.glow` welcome exit 0 | bartis · barket |
| `triple-mold` still refuses | refuse desks |
| wet `pair-mold` under `pair-mold` accepts | nest_type |
| call-body / argv under pair-mold refuse | lower pins |

---

## What stays horizon

| Item | Why held |
|------|----------|
| `%-` call-body under kind/xact/xfer/pair | No struct/union arithmetic demos |
| Argv under tagged / pair molds | Wrong shape for parseInt |
| Ambient gold / free wet polymorphism | Port-order |
| Open `$:` beyond pair (`triple-mold`) | Thicker |
| Sala B0 | Framework / product season |

---

## Files / APIs to change

| Path | Change |
|------|--------|
| `glow/nest_type.rye` | allowlist `pair-mold`; `assert_pair_mold_nests` (two-field `$:`) |
| `glow/lower_bartis.rye` · `glow/lower_barket.rye` | identity emit `PairMold` · baked 5/3 |
| `glow/gen/gate-pair.glow` · `gate-barket-pair.glow` | accept desks |
| refuse desks | retarget to `triple-mold` |
| witnesses · living pins · this brief | STOA126 GREEN when bound |

---

## Witness (after implement)

```bash
rishi/bin/rishi run tools/glow_nest_type_witness.rish
rishi/bin/rishi run tools/glow_barket_lower_witness.rish
rishi/bin/rishi run tools/glow_lower_bartis_witness.rish
```

---

*May every multi-field sample earn its closed struct seat, and may gold wait until freer nesting asks for it.*
