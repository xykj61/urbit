# STOA178 — Sala B0 Glow Gap Inventory

*An Acme Corporation employee reading this gets the sized Phase G ladder from the GREEN Rye Sala B0 fold to a Glow path that can earn the same session root — what already covers, what is missing, and which rung opens next.*

**Language:** EN  
**Version:** `20260721.165732` (UDT · Framework host)  
**Style:** Radiant (see `../context/RADIANT_STYLE.md`)  
**Voice:** Quin  
**Status:** Checkable — inventory seated; STOA179 named  
**Register:** Checkable (Two Rooms) — maps elder surfaces to Glow heads; no rewrite claimed yet  
**Grounds in:** [`20260721-165342_stoa178-sala-b0-glow-thin-face.md`](20260721-165342_stoa178-sala-b0-glow-thin-face.md) · [`../linengrow/sala_b0_fold.rye`](../linengrow/sala_b0_fold.rye) · [`../linengrow/sala_session_core.rye`](../linengrow/sala_session_core.rye) · [`../linengrow/receipt_core.rye`](../linengrow/receipt_core.rye) · Phase G in [`20260719-204650_hoon-core-polymorphism-tame-and-port-order.md`](20260719-204650_hoon-core-polymorphism-tame-and-port-order.md)

**dated_guard:** Do **not** rewrite dated JABS0 / HAWM1 / STOA72–73 records. Elder Rye stays until Glow earns the root. JABS3 stays held. No `deca-*`. No whole-tree sed.

---

## Verdict (one breath)

**SALA_B0_GLOW_GAPS.** The demo fold is a short composition over four Rye vanes. Glow already has multi-field `$:` shapes, jam/cue, cores, gates, and a separate product-root path (expect **5**). It does not yet author FactFields-shaped desks, text/bytes auras, or signing/replay. **STOA179** opens the first citizen desk: a `sala-event-shape` five-face mold matching FactFields names (stand-in `@u32` until `@t`).

---

## Elder fold surface (what must eventually agree)

| Call / type | Role in demo |
|-------------|----------------|
| `kumara.keypair_from_seed` · `PublicKey` | actor identity |
| `mala_core.hex_of_key` · `sign_fact` · `issuer_seed` | hex + sign |
| `receipt_core.FactFields` · `format_log_line` · `parse_*` · `verify_record` · `digest_log` | fact line · root |
| `sala_session_core.append_event` · `replay` | ordered log · root |
| `sala_b0_fold.run_demo_fold` | three events → root `99b3ae96…e4c5fe` |

FactFields faces (all byte slices today): `from` · `to` · `amount` · `memo` · `stamp`.

---

## Glow coverage vs gaps

### Already GREEN (usable nearby)

| Surface | Status | Note |
|---------|--------|------|
| Multi-field `$:` shapes through nona · cast twins | STOA148–176 | Citizen `shape-*` desks |
| jam / cue / nest compose | STOA32–68 | Cell pack, not session log |
| bartis / barket / nest/type | STOA100–121 | Dry gates |
| Product session-root desks · APK import | STOA69–73 | Expect **5** — **not** Sala root hex |
| Asserts `?>` / `?<` | STOA88 | |

### Missing for a Glow-authored fold

| Gap | Kind | Blocks |
|-----|------|--------|
| **FactFields-named event mold** | desk / shape | First citizen spelling of a session event |
| **Text / bounded-bytes aura (`@t` or peer)** | language | Honest `from`/`to`/`memo`/`stamp` (hex · payload · time) |
| **Signing + verify (kumara / mala)** | vane / jet | `append_event` author side |
| **receipt_core fact line + digest** | vane | Log line format · session root |
| **sala_session_core append + replay** | vane | Ordered fold claim |
| **Demo fixture constants in Glow** | desk | Three stamps/payloads + expected hex pin |
| **Glow claim shell** (optional parallel) | product adapter | Owns assert surface while calling elder Rye |

---

## Sized ladder (forward)

| Rung | Lap | Claim |
|------|-----|-------|
| **STOA179** | **GREEN** `20260721.170403` | `shape-sala-event` · `sala-event-shape` · `SalaEventShape`; stand-in `@u32`; cast held |
| **STOA180** | **next** | `@t` / bounded cord (or hex-bytes) aura — retires stand-in debt on text faces |
| later | | Glow claim shell (Rye `run_demo_fold` behind Glow witness) — parallel product, not a substitute for rewrite |
| later | | Thin Glow ports or jets for sign / verify / digest / append / replay — only after shapes + auras |
| held | | **JABS3** inference · economics |

---

## STOA179 thin face (preview)

```glow
+$  sala-event-shape
  $:  from=@u32
      to=@u32
      amount=@u32
      memo=@u32
      stamp=@u32
  ==
```

Stand-in auras keep the desk inside today's cast/nest surface. The brief for STOA179 must say the debt out loud: real FactFields are byte slices; `@t` (or peer) retires the stand-in.

---

## What this inventory does not claim

- A Glow path that already produces `99b3ae96…e4c5fe`
- That STOA69–73 product-root **5** is the Sala rewrite
- That JABS3 opens in this season

---

## Elder witness (baseline)

```bash
rishi/bin/rishi run tools/sala_session_core_witness.rish
```

---

*May every missing head keep its name, and every next lap close one gap the fold actually needs.*
