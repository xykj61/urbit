# STOA127 — Kind-Mold Flip Call-Body

*An Acme Corporation employee reading this gets the thinnest honest call-body deepen under a tagged mold after STOA126: `sample=kind-mold` through closed `%-  flip` on bartis and barket lowers as a KindMold switch with baked `.mint` → `.send`, without ambient gold, argv under kind, call-body under xact/xfer/pair, or Sala B0.*

**Language:** EN  
**Version:** `20260720.190544` (UTC · cloud host)  
**Style:** Radiant (see `../context/RADIANT_STYLE.md`)  
**Voice:** Quin  
**Status:** Checkable — nest · bartis · barket witnesses bind kind-flip accept  
**Register:** Checkable — `glow_nest_type_witness` · `glow_lower_bartis_witness` · `glow_barket_lower_witness`  
**Grounds in:** [`20260720-185502_stoa126-pair-mold-gate.md`](20260720-185502_stoa126-pair-mold-gate.md) · [`20260720-183837_stoa123-kind-mold-gate.md`](20260720-183837_stoa123-kind-mold-gate.md) · [`../glow/gen/mold-kind.glow`](../glow/gen/mold-kind.glow)

**dated_guard:** Do **not** edit the dated STOA126 wording artifact. Seat this lap in *this* brief and living pins when GREEN.

---

## Verdict (one breath)

**KIND_CALL_BODY.** Widen the closed call-body gate set with **`flip`** — the only nontrivial closed gate on `enum(u8) { mint, send }`. Parse admits `|=` / `|^` `sample=kind-mold` / `%-  flip  sample`. Lower emits `KindMold = enum(u8) { mint, send }` and a gate whose body is `switch (sample) { .mint => .send, .send => .mint }`; welcome main bakes `sample = .mint` and checks the gate returns `.send`. `flip` under `@u32` refuses `BodyGateNotYetLowered`. `double` / `inc` / `dec` under `kind-mold` still refuse `BodyGateNotYetLowered`. Call-body under xact / xfer / pair, argv under any named mold, ambient gold, and Sala B0 all stay held.

---

## Why this is the honest next lean A

STOA126 closed the multi-field `$:` gate row. Remaining doors were ambient gold (port-order thick), argv under tagged/pair (wrong shape for parseInt), triple-mold (open `$:` beyond pair), and call-body under a tagged mold. Kind-mold already has a GREEN identity lower and the `.call_one` seam is proven for `@u32`; splicing one closed `enum → enum` gate through that seam is thinner than gold and matches the `double|inc|dec` precedent in shape. `flip` earns its own row because it is a real transformation over the closed alphabet.

---

## What this lap lands (when GREEN)

| Item | Proof |
|------|-------|
| `is_closed_call_gate` admits `flip` | `rune_bartis` · `rune_barket` |
| Kind-mold `flip` lowers to `switch` body · baked `.send` | bartis · barket lower |
| `gate-kind-flip.glow` · `gate-barket-kind-flip.glow` welcome exit 0 | `glow_lower_bartis_witness` · `glow_barket_lower_witness` |
| `double` under kind-mold still refuses `BodyGateNotYetLowered` | lower pins |
| `flip` under `@u32` refuses `BodyGateNotYetLowered` | lower pins |
| triple-mold refuse still holds | refuse desks |

---

## What stays horizon

| Item | Why held |
|------|----------|
| `%-` call-body under `xact` / `xfer` / `pair` | Union / struct arithmetic demos thicker |
| Argv under `kind-mold` | `parseInt(u32)` wrong shape |
| Open `$:` beyond pair (`triple-mold`) | Thicker |
| Ambient gold | Port-order |
| Sala B0 | Framework / product season |

---

## Files / APIs to change

| Path | Change |
|------|--------|
| `glow/rune_bartis.rye` · `glow/rune_barket.rye` | admit `flip`; `is_kind_call_gate` |
| `glow/lower_bartis.rye` · `glow/lower_barket.rye` | `emit_kind_call_body` · KindMold switch |
| `glow/gen/gate-kind-flip.glow` · `gate-barket-kind-flip.glow` | accept desks |
| witnesses · living pins · this brief | STOA127 GREEN when bound |

---

## Witness (after implement)

```bash
rishi/bin/rishi run tools/glow_nest_type_witness.rish
rishi/bin/rishi run tools/glow_barket_lower_witness.rish
rishi/bin/rishi run tools/glow_lower_bartis_witness.rish
```

---

*May every closed alphabet earn its one honest transformation, and may gold wait until freer nesting asks for it.*
