# STOA227 — Demo Line Emit from Seated Pins (Lean C, thin)

*An Acme Corporation employee reading this gets the first runtime emit slice after rest: the elder demo log line is formatted from seated sig/fact hex pins (desk-bound), proven byte-identical to `claim_demo_line`.*

**Language:** EN  
**Version:** `20260722.132533` (EDT · Framework host)  
**Style:** Radiant (see `../context/RADIANT_STYLE.md`)  
**Voice:** Quin  
**Status:** Checkable — GREEN `20260722.132533`  
**Register:** Checkable (Two Rooms) — pin emit GREEN; `.glow` file not read at runtime  

**Grounds in:** STOA226 lean **C** (sized thin) · STOA225 pins · desk bind

**dated_guard:** Do **not** claim product reads `sample-demo-fact-line-lits.glow` at runtime. Do **not** claim fold uses pin emit yet. JABS3 held.

---

## Verdict (one breath)

**DEMO_PIN_EMIT.** `format_demo_line_from_pins` · `demo_pin_emit_agrees_claim` · elder line from seated hex · equals live sign path.

---

## Witness

```bash
export RYE_ZIG=vendor/zig-toolchain/zig
rishi/bin/rishi run tools/glow_sala_b0_line_witness.rish
```

---

## Hold

Wire append/fold demo path to pin emit. Runtime `.glow` file parse. Fixture pin emit twin. JABS3.
