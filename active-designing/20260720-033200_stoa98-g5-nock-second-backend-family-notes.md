# STOA98 / G5 — Nock Second-Backend Notes Beside Truth Families

*An Acme Corporation employee reading this gets the thin G5 seat: which GREEN Glow rune families already touch ambient truth (or will touch jets), and what the Nock second backend means beside each — without claiming Nock emit is live.*

**Language:** EN  
**Version:** `20260720.033200` (UTC · cloud host)  
**Style:** Radiant (see `../context/RADIANT_STYLE.md`)  
**Voice:** Quin  
**Status:** Checkable — living pin in `context/TAME_GUIDANCE.md`; bound by `tools/glow_g5_nock_family_notes_witness.rish`  
**Register:** Mixed (Two Rooms) — family list is design until the witness binds the pin strings  
**Grounds in:** [`20260719-204001_glow-rune-pronunciation-alphabet-riscv-and-generators.md`](20260719-204001_glow-rune-pronunciation-alphabet-riscv-and-generators.md) **G5** · [`../context/specs/20260717-154943_glow-truth-zig-ambient-nock-loobean-seam.md`](../context/specs/20260717-154943_glow-truth-zig-ambient-nock-loobean-seam.md) · [`../nock/README.md`](../nock/README.md) · [`../glow/truth_semantics.rye`](../glow/truth_semantics.rye)

**dated_guard:** Do **not** edit the dated `20260719-204001_*` or `20260719-220814_*` files. Seat G5 in this brief and in living TAME / glow pins.

---

## Verdict (one breath)

Primary path stays Glow→Rye→Zig→**RISC-V**. **Nock** is the second backend for Urbit interop and verification only. G5 names that fact beside every GREEN family that already touches **truth** (and notes that **jet**-touching families are still horizon). Nock emit of Glow desks stays unopened — when it opens, these notes become the teaching seam, not a surprise rewrite of ambient Zig `bool`.

---

## Families that touch truth today

| Family | GREEN heads | Primary (RISC-V / Zig) | Nock second-backend note |
|--------|-------------|------------------------|---------------------------|
| **wut** | `?:` · `?-` · `?~` · `?>` · `?<` | Ambient Zig `bool` · Rye `assert` · POSIX exits | Loobean polarity (0 = yes) lives only inside `nock/` and at `truth_semantics` conversion; a future Nock emit of wut heads must convert at the seam, never invert Glow ambient |

No other GREEN family currently carries ambient truth tests. Cast (**ket**), molds (**buc**), cells (**col**), calls (**cen**), cores (**bar**), binds (**tis**), imports (**fas**) keep RISC-V/TAME notes in the G1 table; they do not redefine yes/no.

---

## Jets

Hoon jets accelerate Nock. Glow's primary path does not emit Nock today, so **no GREEN family is jet-touching yet**. When a jet table or Glow→Nock emit lap opens, add that family here and in the TAME G5 pin in the same change — or the G5 witness goes red.

Horizon pointer: `nock/` already names interop/verification; RISC-V path already carries its own keystone jets under revival briefs — those are not Glow rune-family jets.

---

## What this lap does not do

- Open Glow→Nock desk emit
- Rewrite the immutable G1 pronunciation table rows
- Seat bartis `|=` or Sala B0
- Grow whole `glow/` onto the style-scan roster

---

## Witness

```bash
rishi/bin/rishi run tools/glow_g5_nock_family_notes_witness.rish
rishi/bin/rishi run tools/glow_truth_semantics_witness.rish
```

---

*May every yes in Glow stay Zig's yes, and may Nock's own yes wait at the named door until emit earns its seat.*
