# STOA198 — Glow Fold Product Surface

*An Acme Corporation employee reading this gets the Glow-named fold peer to elder `sala_b0_fold`: `run_demo_fold` is the composed claim path, and NativeActivity calls it by that name.*

**Language:** EN  
**Version:** `20260721.183554` (UDT · Framework host)  
**Style:** Radiant (see `../context/RADIANT_STYLE.md`)  
**Voice:** Quin  
**Status:** Checkable — GREEN `20260721.183554`  
**Register:** Checkable (Two Rooms) — product fold GREEN; pack GREEN; Glow-authored append/sign/replay bodies held  

**Grounds in:** [`20260721-183253_stoa197-claim-compose-append-replay.md`](20260721-183253_stoa197-claim-compose-append-replay.md) · STOA191 claim wire

**dated_guard:** Do **not** rewrite elder `sala_b0_fold` internals. STOA191 claim-direct wire is superseded for the product entry by this fold surface. JABS3 held.

---

## Verdict (one breath)

**GLOW_FOLD.** `linengrow/glow_sala_b0_fold.rye` exposes `run_demo_fold` → `claim_demo_fold` (append+replay compose). NativeActivity imports the Glow fold. Witness agrees elder `run_demo_fold`. `stoa198_native_fold_wire_witness` pack GREEN.

---

## Witness

```bash
rishi/bin/rishi run tools/glow_sala_b0_fold_witness.rish
rishi/bin/rishi run tools/stoa198_native_fold_wire_witness.rish
```

---

## Hold

Glow-authored append/sign/replay bodies (replace elder vane internals). Pixel eyes. JABS3.

---

*May every product fold name its compose path before the bodies earn Glow authorship.*
