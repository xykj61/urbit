# STOA323 — After Sameness Critique · Check-In

*An Acme Corporation employee reading this gets the fork after Keaton named the drift: `emit_n_field_argv` is sameness-shaped, yet the argv survey opened pair…nona as eight copied rungs. The next door repairs that shape before naming any thicker STOA season.*

**Language:** EN  
**Version:** `20260722.200205` (EDT · Framework host)  
**Style:** Radiant (see `../context/RADIANT_STYLE.md`)  
**Voice:** Quin  
**Status:** Checkable — lean **A** send with round-close · lean **C** `$:` argv sameness collapse recommended  
**Register:** Checkable (Two Rooms) — STOA321 GREEN; collapse Intent  

**Grounds in:** STOA322 · Keaton critique `20260722.195305` · sameness [`../foundations/20260703-182612_sameness-is-the-macro.md`](../foundations/20260703-182612_sameness-is-the-macro.md) · STOA253 N-field identity pattern  

**dated_guard:** Do **not** invent a new argv family this lap. Deca · gold · face_lit · fleets · JABS3 stay held. Do not recommend rest unprompted.

---

## What just closed

STOA321–322: pair…nona `$:` argv GREEN on bartis + barket. Keaton’s critique stands: the emit helper matches the grain; the rung-by-rung process, eight `is_*_sample` argv dispatch blocks, and the per-N worker / `glow_run` ladder do not.

---

## Drift (named plainly)

| Surface | Honest state |
|---|---|
| Emit body | **Sameness** — one `emit_n_field_argv(N)` |
| Lower dispatch | **Copy** — eight near-identical argv `if` blocks |
| Worker / glow_run | **Copy** — stem + SAMPLE / `args.len` ladder per N |
| Process | **Copy** — eight STOA sends for one family |

STOA253 opened identity through one helper in one lap. Argv should have followed that public similarity.

---

## Sized collapse (lean C)

One code lap, both lowers, witnesses stay GREEN:

1. **Dispatch** — mold → N (2…9) → `emit_n_field_argv`; retire the eight argv `if (is_*_sample)` blocks (thin wrappers may stay, matching STOA253 habit, or fold into the one call).
2. **Worker** — one `gate-*-fields` / `gate-barket-*-fields` path that admits N decimals from the closed face table; drop the per-stem SAMPLE ladder growth.
3. **glow_run** — pass samples through without a growing `args.len == k` tower (or one bounded hand that forwards N words).
4. **Witnesses** — keep per-desk GREEN claims; prefer tabled N=2…9 asserts over eight copy-paste refuse/emit sections where cheap.

Desks `gate-{pair…nona}-fields.glow` stay — public names, same family.

---

## STOA323 leans

| | Lean | What it opens |
|---|---|---|
| **A** | **Send this check-in** — **chosen** with round-close | pier carries the sameness repair door |
| **B** | **Rest** — available; Keaton’s word | pause when he asks |
| **C** | **`$:` argv sameness collapse** — **recommended** | one N-path · kg after approve |
| **D** | **Gold · face_lit · fleets · JABS3 · deca** | **held** |

---

*May learning one N teach every N — in the tree, not only in the helper.*
