# STOA300 — Tagged Argv Survey · Kind Unit-Tag Reader

*An Acme Corporation employee reading this gets the Survey for gate argv under tagged molds after flip emit closed: amount/count stay on `parseInt(u32)`, kind earns a unit-tag reader (`mint`/`send`), and xact/xfer/pair argv stay refused until a richer sample family arrives.*

**Language:** EN  
**Version:** `20260722.161229` (EDT · Framework host)  
**Style:** Radiant (see `../context/RADIANT_STYLE.md`)  
**Voice:** Quin  
**Status:** Checkable — survey seated · kind argv GREEN `20260722.161609`  
**Register:** Checkable (Two Rooms) — kind argv helpers + witnesses; payload/pair argv Intent  

**Grounds in:** STOA299 lean **A** · STOA132–133 amount/count argv · STOA293–297 tagged flip families  

**dated_guard:** Do **not** force KindShape through `parseInt(u32)`. Do **not** open xact/xfer/pair argv this lap. Ambient gold · fleets · JABS3 stay held.

---

## Survey — where argv lives

| Sample mold | Welcome | Argv today | Reader |
|---|---|---|---|
| `@u32` · amount · count | GREEN | GREEN | `parseInt(u32)` |
| **kind** (unit `$%`) | GREEN | **GREEN** STOA300 | tag word `mint`/`send` |
| xact · xfer (payload `$%`) | GREEN | refuse `SampleDoesNotNest` | held |
| pair…nona (`$:` N-field) | GREEN | refuse `SampleDoesNotNest` | held |

**Refuse site (both lowers):** `emit_argv` — kind carved out for identity; payload + N-field stay on the guard.

**Two argv families (do not force one):**

| Family | Members | Shape |
|---|---|---|
| **u32 decimal** | aura · amount · count | `parseInt(u32, argv[1], 10)` |
| **Unit tag** | kind | `mint` / `send` string → `KindShape` |

---

## First code lap (this GREEN)

`emit_kind_argv` on **bartis + barket** · desks `gate-kind-tag.glow` · `gate-barket-kind-tag.glow` · worker stems · both lower witnesses GREEN · xact/xfer argv refuse pinned.

| Later | Claim |
|---|---|
| Xact / xfer argv | payload tag + faces — thicker reader |
| Pair…nona argv | multi-field `$:` — thicker reader |
| Kind flip argv | call-body under kind + argv — optional |
| Held | gold · face_lit package · fleets · JABS3 · `deca-*` |

---

## Witness

```bash
export RYE_ZIG=vendor/zig-toolchain/zig
rishi/bin/rishi run tools/glow_lower_bartis_witness.rish
rishi/bin/rishi run tools/glow_barket_lower_witness.rish
rishi/bin/rishi run tools/glow_run.rish glow/gen/gate-kind-tag.glow mint
```

---

*May every argv family name its reader before it forces one parse.*
