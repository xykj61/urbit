# Proven-seat G1 — Claude three asks (toolchain · kg unroll · module park)

*Claude walked tip `130d374` — signatures Good on `ee1c37f`, `b790881`, and the fill — read the crossing pin in full, and answered the evening handoff asks. This note seats the rulings.*

**Stamp:** `20260712.201200`
**Language:** EN
**Style:** Radiant
**Voice:** Rio 3 recording Claude · Kaeden’s clock from the 20:12 paste
**Status:** Checkable — three asks seated · sources pin **revised** `201802` to Codeberg **26.05** · toolchain 25.05 stands
**Ground:** evening handoff [`../bron-resins/20260712-200445_claude-evening-g1-toolchain.bron`](../bron-resins/20260712-200445_claude-evening-g1-toolchain.bron) · crossing pin [`20260712-195339_proven-seat-g1-crossing-pin.md`](20260712-195339_proven-seat-g1-crossing-pin.md) · opening counsel [`20260712-195155_proven-seat-g1-claude-opening-counsel.md`](20260712-195155_proven-seat-g1-claude-opening-counsel.md)

---

## Day sentence (Claude)

**Metal parity GREEN at `193958`** — the first debt, standing since before the morning fold, is paid and paid *chaptered*. A parity-first sitting that opened and closed green is exactly what the park law promised.

---

## Ask one — affirmed (no park)

Publisher’s own rule: releases require tool chains with **equal or next smaller** version number. For the 26.x line beneath Sculpt **26.04**, that is precisely **25.05**. The 25.05 announcement seals a 24-month window (GCC 14.2 · binutils 2.44) across the whole 26.x era. Master’s `tool/tool_chain` still pins `TOOL_CHAIN_VERSION` 25.05 · GCC 14.2.0 — matching the fetched `genode-x86-g++` report. Goa raises no mismatch. **No park.**

**Pairing word for G1:** when the guest build pulls Genode sources, pin them at **25.05** beside the 25.05 toolchain — the release’s own tested pair. The Sculpt-matching *source* question belongs to the later rung that touches the Sculpt ground itself.

**META trio (visible pairing):**

| Field | Pin |
|-------|-----|
| toolchain | **25.05** |
| sources | **25.05** |
| ground | **Sculpt 26.04** |

Delight to relay: 25.05’s headline is an API uplift that encodes every error condition as sum types — the teacher on the far bank walking toward TAME’s named-errors law on its own.

---

## Ask two — affirmed (no reorder · first motion named)

The toolchain alone cannot link a posix/libc component. The crossing needs the Genode source tree — `repos/base` plus `repos/libports`, where libc lives behind `tool/ports/prepare_port libc` (the teacher hash-verifies its own upstream downloads).

**Kg unroll:**

1. **Source fetch witness** — same shape as the toolchain witness · pin **25.05** tag · under `tools/.cache/proven-seat/` (cache claims no seat)
2. **Prepare libc port** — `tool/ports/prepare_port libc`
3. **`create_builddir` for x86_64**
4. **Guest** — SLC: smallest honest receipt-verify · posix main via `Libc::Component::construct` · one signed receipt fixture · own GREEN line on serial

Kumara-grade depth may arrive as a later lap if the first rung wants only digest-grade verify. Jailed TCG needs no lane — no `/dev/kvm` — so serial GREEN can land inside ai-jail on any machine; KVM acceleration stays an optional host luxury later through the gate that already exists.

---

## Ask three — park holds

Provisional home needs no word: the source clone rests under the same cache shelf as the toolchain. The permanent-home round opens after the first serial GREEN — then `vendor/` carries the shaped precedent (zig lives there for the identical reason: build-needed third-party source, kept local and whole). Proposal on the shelf; word Kaeden’s.

---

## Host note (Claude returned)

zsh ships with interactive comments off, so a pasted `# …` becomes arguments; `setopt interactive_comments` in `~/.zshrc` makes pasted comment lines inert, bash-style.

---

*May the sources rest beside their toolchain, the smallest guest speak first, and the next GREEN arrive in the seat’s own voice.*
