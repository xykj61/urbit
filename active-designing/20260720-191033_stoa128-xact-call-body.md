# STOA128 — Xact-Mold Flip Call-Body

*An Acme Corporation employee reading this gets the thinnest honest call-body deepen under a payload `$%` after STOA127: `sample=xact-mold` through closed `%-  flip` on bartis and barket lowers as an XactMold switch with baked `.mint` amount 5 → `.send`, without ambient gold, argv under xact, call-body under xfer/pair, or Sala B0.*

**Language:** EN  
**Version:** `20260720.191033` (UTC · cloud host)  
**Style:** Radiant (see `../context/RADIANT_STYLE.md`)  
**Voice:** Quin  
**Status:** Checkable — nest · bartis · barket witnesses bind xact-flip accept  
**Register:** Checkable — `glow_nest_type_witness` · `glow_lower_bartis_witness` · `glow_barket_lower_witness`  
**Grounds in:** [`20260720-190544_stoa127-kind-call-body.md`](20260720-190544_stoa127-kind-call-body.md) · [`20260720-184304_stoa124-xact-mold-gate.md`](20260720-184304_stoa124-xact-mold-gate.md) · [`../glow/gen/mold-xact.glow`](../glow/gen/mold-xact.glow)

**dated_guard:** Do **not** edit the dated STOA127 wording artifact. Seat this lap in *this* brief and living pins when GREEN.

---

## Verdict (one breath)

**XACT_CALL_BODY.** Widen the closed call-body gate set for xact-mold with **`flip`** — the thinnest nontrivial closed gate on `union(enum) { mint: struct { amount: u32 }, send }`, mirroring the kind-mold flip shape. Parse admits `|=` / `|^` `sample=xact-mold` / `%-  flip  sample`. Lower emits `XactMold` and a gate whose body is `switch (sample) { .mint => .send, .send => .{ .mint = .{ .amount = 0 } } }`; welcome main bakes `sample = .mint { amount: 5 }` and checks the gate returns `.send`. `double` / `inc` / `dec` under xact-mold still refuse. `flip` under `@u32`, xfer-mold, and pair-mold refuse. Call-body under xfer / pair, argv under any named mold, ambient gold, and Sala B0 all stay held.

---

## Why this is the honest next lean A

STOA127 closed the unit `$%` call-body row. Remaining doors were ambient gold (port-order thick), argv under tagged/pair, triple-mold, and call-body under payload/multi-face molds. Xact-mold already has a GREEN identity lower; splicing `flip` through the seated `.call_one` seam is thinner than gold and mirrors kind flip without inventing open polymorphism.

---

## What this lap lands (when GREEN)

| Item | Proof |
|------|-------|
| `is_xact_call_gate` admits `flip` | `rune_bartis` · `rune_barket` |
| Xact-mold `flip` lowers to `switch` body · baked `.send` | bartis · barket lower |
| `gate-xact-flip.glow` · `gate-barket-xact-flip.glow` welcome exit 0 | witnesses |
| `double` under xact-mold still refuses | lower pins |
| `flip` under xfer / pair / `@u32` refuses | lower pins |

---

## What stays horizon

| Item | Why held |
|------|----------|
| `%-` call-body under `xfer` / `pair` | Multi-face / struct demos thicker |
| Argv under `xact-mold` | `parseInt(u32)` wrong shape |
| Open `$:` beyond pair (`triple-mold`) | Thicker |
| Ambient gold | Port-order |
| Sala B0 | Framework / product season |

---

## Files / APIs to change

| Path | Change |
|------|--------|
| `glow/rune_bartis.rye` · `glow/rune_barket.rye` | `is_xact_call_gate` |
| `glow/lower_bartis.rye` · `glow/lower_barket.rye` | `emit_xact_call_body` · XactMold switch |
| `glow/gen/gate-xact-flip.glow` · `gate-barket-xact-flip.glow` | accept desks |
| witnesses · living pins · this brief | STOA128 GREEN when bound |

---

## Witness (after implement)

```bash
rishi/bin/rishi run tools/glow_nest_type_witness.rish
rishi/bin/rishi run tools/glow_barket_lower_witness.rish
rishi/bin/rishi run tools/glow_lower_bartis_witness.rish
```

---

*May every payload alphabet earn its one honest flip, and may gold wait until freer nesting asks for it.*
