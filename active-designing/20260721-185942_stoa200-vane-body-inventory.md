# STOA200 — Vane-Body Inventory (Lean B)

*An Acme Corporation employee reading this gets an honest map of what still runs as elder Rye inside the Glow-named Sala B0 fold, what Glow already owns as product surface, and which first body lap is sized next.*

**Language:** EN  
**Version:** `20260721.185942` (UDT · Framework host)  
**Style:** Radiant (see `../context/RADIANT_STYLE.md`)  
**Voice:** Quin  
**Status:** Checkable — inventory seated `20260721.185942`  
**Register:** Checkable (Two Rooms) — maps wrappers vs bodies; no Glow-authored vane body claimed  

**Grounds in:** lean **B** in [`20260721-185137_stoa199-after-glow-fold-checkin.md`](20260721-185137_stoa199-after-glow-fold-checkin.md) · [`20260721-165732_stoa178-sala-b0-glow-gap-inventory.md`](20260721-165732_stoa178-sala-b0-glow-gap-inventory.md) · Pixel eyes [`20260721-185805_stoa199a-pixel-glow-fold-eyes.md`](20260721-185805_stoa199a-pixel-glow-fold-eyes.md)

**dated_guard:** Do **not** claim the Pixel HAWM1 hex was produced by Glow-rune fold bodies. Prefer a named sub-lean before the first body code lap. JABS3 held.

---

## Verdict (one breath)

**VANE_BODIES.** Product path is Glow-*named* and composed; vane *bodies* (sign, format, append guts, verify, replay guts, digest guts) remain elder Rye. Inventory sizes three forward doors. Recommended first body lap: **thin sign/verify product surface** (Rye wrap over elder, parallel to digest/append) — not a Glow-rune rewrite yet.

---

## Call stack today (Pixel / `glow_sala_b0_fold`)

```
NativeActivity
  → glow_sala_b0_fold.run_demo_fold          # STOA198 product name
    → glow_sala_b0_claim.claim_demo_fold     # STOA197 compose
      → glow_sala_b0_replay.claim_replay…    # STOA196 wrap
        → glow_sala_b0_append.append_demo…   # STOA195 wrap + fixture pins
          → sala.append_event                # ELDER body (sign + format + copy)
        → sala.replay (×2)                   # ELDER body (parse + verify + order + digest)
      → fixture / fold root pin              # STOA193 · elder hex table
```

Digest also exists as `glow_sala_b0_digest` (STOA194) over elder `receipt_core.digest_log` — used by append claim path; replay’s root still digests inside elder `replay`.

---

## Wrapper vs body

| Surface | Glow product wrap | Elder body still owns |
|---------|-------------------|------------------------|
| Demo stamps / memos / root pin | `glow_sala_b0_demo_fixture` · shape desk roles | literals also inside `sala_b0_fold` |
| Digest SHA3-256 | `glow_sala_b0_digest.claim_digest_log` | `receipt_core.digest_log` |
| Append three events | `glow_sala_b0_append.append_event` | `sala_session_core.append_event` → `mala.sign_fact` · `format_log_line` |
| Replay verify/order/root | `glow_sala_b0_replay.replay` | `sala_session_core.replay` → parse · verify · digest |
| Claim / fold name | `glow_sala_b0_claim` · `glow_sala_b0_fold` | peer baseline `sala_b0_fold.run_demo_fold` |
| FactFields shape (Glow desks) | `sala-event-shape` · fixture-constants shape | runtime FactFields still byte slices in elder |

---

## What “Glow-authored body” would mean (honest)

| Body | Glow-authored means | Blockers today |
|------|---------------------|----------------|
| **sign / verify** | Glow jet or desk that produces/checks ed25519 over canonical fact | No Glow crypto jet; kumara/mala stay Rye |
| **format / parse log line** | Glow spelling of sig+fact hex line | Needs cord/hex emit at line scale; still Rye formatters |
| **append** | Glow gate composing sign→format→copy into log | Needs sign + format first |
| **replay** | Glow gate composing parse→verify→order→digest | Needs verify + digest + parse |
| **digest** | Glow jet for SHA3-256 of log bytes | Zig hash underneath any name |

Until jets exist, every “body” lap that stays shippable is a **product surface wrap** (like STOA194–196), not a rune-authored vane.

---

## STOA200 sub-leans (first body after inventory)

| | Sub-lean | What it opens |
|---|---|---|
| **B1** | **recommended** | Thin **sign/verify product surface** (`glow_sala_b0_sign` over elder `sign_fact` / `verify_record`); witness on demo fact; still Rye |
| **B2** | | Thin **format/parse log-line surface** over elder `format_log_line` / `parse_log_line` |
| **B3** | | Glow **desk-only** deepen (fixture literals / fact-line shape) — language, not vane body |
| **B4** | | Hold body season — rest until a jet/crypto word |

---

## Witness of this lap

Inventory brief itself — no new binary. Prior GREEN suite still stands:

```bash
rishi/bin/rishi run tools/stoa198_native_fold_wire_witness.rish
```

---

## Hold

Glow-rune vane rewrite. Crypto jets. JABS3. Silent kg past B1 without a word.

---

*May every wrap name the elder body it still calls, until a jet earns the Glow name.*
