# STOA124 — Xact-Mold Gate Sample (Payload `$%` Identity)

*An Acme Corporation employee reading this gets the thinnest honest payload `$%` gate-sample deepen after STOA123: `sample=xact-mold` on bartis and barket lowers as a closed `XactMold` union identity with baked `%mint` amount 5, without ambient gold, argv, call-body, or Sala B0.*

**Language:** EN  
**Version:** `20260720.184304` (UTC · cloud host)  
**Style:** Radiant (see `../context/RADIANT_STYLE.md`)  
**Voice:** Quin  
**Status:** Checkable — nest · bartis · barket witnesses bind xact-mold accept  
**Register:** Checkable — `glow_nest_type_witness` · `glow_lower_bartis_witness` · `glow_barket_lower_witness` 
**Grounds in:** [`20260720-183837_stoa123-kind-mold-gate.md`](20260720-183837_stoa123-kind-mold-gate.md) · [`../glow/gen/mold-xact.glow`](../glow/gen/mold-xact.glow)

**dated_guard:** Do **not** edit the dated STOA123 wording artifact. Seat this lap in *this* brief and living pins when GREEN.

---

## Verdict (one breath)

**XACT_MOLD_GATE.** Widen the closed gate-sample allowlist with **`xact-mold`**. Parse accepts `|=` / `|^` `sample=xact-mold` identity. Lower emits `XactMold = union(enum) { mint: struct { amount: u32 }, send }` and a welcome main baked to `.mint = .{ .amount = 5 }`. Call-body and argv under xact-mold stay refused. Multi-face payload `$%` (`xfer-mold`) stays refuse. Ambient gold · Sala B0 held.

---

## Why this is the honest next lean A

STOA123 closed unit `$%` gate identity. Remaining doors were gold vs payload `$%`. `xact-mold` already has a GREEN mold lower (`union(enum)`); wiring it as a gate sample is thinner than ambient gold polymorphism.

---

## What this lap lands (when GREEN)

| Item | Proof |
|------|-------|
| Allowlist includes `xact-mold` | nest_type |
| `gate-xact.glow` · `gate-barket-xact.glow` welcome exit 0 | bartis · barket |
| `xfer-mold` still refuses | refuse desks |
| wet `xact-mold` under `xact-mold` accepts | nest_type |
| call-body / argv under xact-mold refuse | lower pins |

---

## What stays horizon

| Item | Why held |
|------|----------|
| `%-` call-body under xact-mold | No union arithmetic demos |
| Argv under xact-mold | Wrong shape for parseInt |
| Multi-face payload `$%` (`xfer-mold`) | Thicker |
| Ambient gold | Port-order |
| Sala B0 | Framework / product season |

---

## Files / APIs to change

| Path | Change |
|------|--------|
| `glow/nest_type.rye` | allowlist `xact-mold`; `assert_xact_mold_nests` |
| `glow/lower_bartis.rye` · `glow/lower_barket.rye` | identity emit `XactMold` · baked mint/5 |
| `glow/gen/gate-xact.glow` · `gate-barket-xact.glow` | accept desks |
| refuse desks | retarget to `xfer-mold` |
| witnesses · living pins · this brief | STOA124 GREEN when bound |

---

## Witness (after implement)

```bash
rishi/bin/rishi run tools/glow_nest_type_witness.rish
rishi/bin/rishi run tools/glow_barket_lower_witness.rish
rishi/bin/rishi run tools/glow_lower_bartis_witness.rish
```

---

*May every payload sample earn its closed union seat, and may gold wait until freer nesting asks for it.*
