# STOA180 — After Sala-Event-Shape · `@t` / Cord Aura Check-In

*An Acme Corporation employee reading this gets the check-in after `sala-event-shape` seats FactFields names with stand-in `@u32`: how Glow retires that typed debt without opening a whole vane rewrite.*

**Language:** EN  
**Version:** `20260721.171030` (UDT · Framework host)  
**Style:** Radiant (see `../context/RADIANT_STYLE.md`)  
**Voice:** Quin  
**Status:** Checkable — awaiting lean  
**Register:** Mixed (Two Rooms) — STOA179 GREEN; text/bytes aura is a language seam  
**Grounds in:** [`20260721-170403_stoa179-sala-event-shape-desk.md`](20260721-170403_stoa179-sala-event-shape-desk.md) · [`20260721-165732_stoa178-sala-b0-glow-gap-inventory.md`](20260721-165732_stoa178-sala-b0-glow-gap-inventory.md) · aura scoping [`../external-research/20260713-225841_glow-supplement-scoping-runes-auras-jet-state.md`](../external-research/20260713-225841_glow-supplement-scoping-runes-auras-jet-state.md)

**dated_guard:** Do **not** rewrite dated `@uN`-only aura claims as if `@t` already lowered. Elder Rye FactFields stay byte slices. Capacity freeze at nine `$:` faces stays. JABS3 stays held.

---

## What just closed

Sala B0 Glow season opened (STOA177 lean A). Gap inventory sized the ladder (STOA178). First citizen desk `sala-event-shape` → `SalaEventShape` with faces `from` · `to` · `amount` · `memo` · `stamp` (STOA179). Faces stand in as `@u32`; real FactFields are text/bytes.

---

## STOA180 leans

| | Lean | What it opens |
|---|---|---|
| **A** | **recommended** | **Thin `@t` with a named byte bound** — tokenize · mold-face accept · nest/type bound; lower emit follows in a second lap if needed. TAME-shaped cord, not unbound Hoon trust |
| **B** | | **`@ux` / hex-bytes peer first** — pubkey-shaped `from`/`to` before general cord |
| **C** | | **Stand-in cast twins** for `sala-event-shape` while `@u32` debt remains |
| **D** | | **Glow claim shell** — product adapter calling elder Rye `run_demo_fold` |
| **E** | | **Rest** — shape desk is enough until a later season |

---

## Why A first

The inventory named text/bytes as the blocking aura gap. A thin `@t` with an explicit max length matches Glow’s aura-bound discipline (every aura names its bound) and unblocks honest `memo` · `stamp` · hex faces without pretending the fold is rewritten. Hex-only `@ux` (B) can follow once `@t` peeks exist, or replace A if Keaton wants pubkey faces first.

---

*May every cord name its bound, and may stand-in numbers never speak for a session line.*
