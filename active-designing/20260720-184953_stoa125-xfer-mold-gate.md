# STOA125 — Xfer-Mold Gate Sample (Multi-Face Payload `$%` Identity)

*An Acme Corporation employee reading this gets the thinnest honest multi-face payload `$%` gate-sample deepen after STOA124: `sample=xfer-mold` on bartis and barket lowers as a closed `XferMold` union identity with baked `%mint` from 5 / amount 3, without ambient gold, argv, call-body, or Sala B0.*

**Language:** EN  
**Version:** `20260720.184953` (UTC · cloud host)  
**Style:** Radiant (see `../context/RADIANT_STYLE.md`)  
**Voice:** Quin  
**Status:** Checkable — nest · bartis · barket witnesses bind xfer-mold accept  
**Register:** Checkable — `glow_nest_type_witness` · `glow_lower_bartis_witness` · `glow_barket_lower_witness` 
**Grounds in:** [`20260720-184304_stoa124-xact-mold-gate.md`](20260720-184304_stoa124-xact-mold-gate.md) · [`../glow/gen/mold-xfer.glow`](../glow/gen/mold-xfer.glow)

**dated_guard:** Do **not** edit the dated STOA124 wording artifact. Seat this lap in *this* brief and living pins when GREEN.

---

## Verdict (one breath)

**XFER_MOLD_GATE.** Widen the closed gate-sample allowlist with **`xfer-mold`**. Parse accepts `|=` / `|^` `sample=xfer-mold` identity. Lower emits `XferMold = union(enum) { mint: struct { from: u32, amount: u32 }, send }` and a welcome main baked to `.mint = .{ .from = 5, .amount = 3 }`. Call-body and argv under xfer-mold stay refused. Multi-field `$:` `pair-mold` stays refuse (not gate-welcome). Ambient gold · Sala B0 held.

---

## Why this is the honest next lean A

STOA124 closed one-face payload `$%` gate identity. Remaining doors were gold vs multi-face payload `$%`. `xfer-mold` already has a GREEN mold lower (`union(enum)` with two faces); wiring it as a gate sample is thinner than ambient gold polymorphism.

---

## What this lap lands (when GREEN)

| Item | Proof |
|------|-------|
| Allowlist includes `xfer-mold` | nest_type |
| `gate-xfer.glow` · `gate-barket-xfer.glow` welcome exit 0 | bartis · barket |
| `pair-mold` still refuses | refuse desks |
| wet `xfer-mold` under `xfer-mold` accepts | nest_type |
| call-body / argv under xfer-mold refuse | lower pins |

---

## What stays horizon

| Item | Why held |
|------|----------|
| `%-` call-body under kind/xact/xfer | No union arithmetic demos |
| Argv under kind/xact/xfer | Wrong shape for parseInt |
| Ambient gold / free wet polymorphism | Port-order |
| Sala B0 | Framework / product season |

---

## Files / APIs to change

| Path | Change |
|------|--------|
| `glow/nest_type.rye` | allowlist `xfer-mold`; `assert_xfer_mold_nests` (multi-face) |
| `glow/lower_bartis.rye` · `glow/lower_barket.rye` | identity emit `XferMold` · baked mint/5/3 |
| `glow/gen/gate-xfer.glow` · `gate-barket-xfer.glow` | accept desks |
| refuse desks | retarget to `pair-mold` |
| witnesses · living pins · this brief | STOA125 GREEN when bound |

---

## Witness (after implement)

```bash
rishi/bin/rishi run tools/glow_nest_type_witness.rish
rishi/bin/rishi run tools/glow_barket_lower_witness.rish
rishi/bin/rishi run tools/glow_lower_bartis_witness.rish
```

---

*May every multi-face sample earn its closed union seat, and may gold wait until freer nesting asks for it.*
