# STOA180 — Thin `@t` Cord Aura (Lean A)

*An Acme Corporation employee reading this gets the first TAME-shaped cord aura: `@t` tokenizes, nests under `@t` with a named byte bound, and parses on `$:` faces — while cord lower stays held for the next lap.*

**Language:** EN  
**Version:** `20260721.171800` (UDT · Framework host)  
**Style:** Radiant (see `../context/RADIANT_STYLE.md`)  
**Voice:** Quin  
**Status:** Checkable — bound by tokens · nest_type · rune_mold · lower_mold witnesses  
**Register:** Checkable (Two Rooms) — thin `@t` GREEN; cord emit held; shape vocabulary kept  
**Grounds in:** [`20260721-171030_stoa180-at-aura-checkin.md`](20260721-171030_stoa180-at-aura-checkin.md) · [`20260720-223226_glow-os-shape-not-mold.md`](20260720-223226_glow-os-shape-not-mold.md)

**dated_guard:** Do **not** claim `@t` faces lower to runnable desks yet. Do **not** rewrite dated `@uN`-only aura claims. Keep **shape** as Glow OS word — **type** stays refused; **norm** stays held. `MoldSpec` / Zig `*Mold` API rename stays a separate seam.

---

## Verdict (one breath)

**CORD_AURA_THIN.** Seat lean A. Lex `@t`. Nest `@t` under `@t` with `aura_t_max_bytes = 256`. Parse `$:` `face=@t` (cord flag). Lower refuses `CordAuraNotYetLowered`. Living pins lead with **shape** (mold = study). Keaton affirmed shape over type (`20260721.171644`).

---

## What this lap lands

| Item | Proof |
|------|-------|
| `@t` tokenize | `glow_tokens_witness` |
| `@t` nest · bound 256 | `nest_type_witness` |
| `$:` `memo=@t` parse | `glow_rune_mold_witness` |
| Cord lower refuse | `glow_lower_mold_witness` |
| Shape-primary README / nest gloss | living pins |

---

## What stays next

| Item | Why |
|------|-----|
| **STOA181** `@t` lower emit | bounded cord field in Zig / demo |
| Retire stand-in `@u32` on `sala-event-shape` text faces | after lower |
| `MoldSpec` → ShapeSpec Zig rename | large seam · separate |
| JABS3 | held companion |

---

## Witness

```bash
rishi/bin/rishi run tools/glow_tokens_witness.rish
rishi/bin/rishi run tools/glow_rune_mold_witness.rish
rishi/bin/rishi run tools/glow_lower_mold_witness.rish
# nest_type binary covers @t nest (full .rish also pulls barket/bartis siblings)
glow/bin/nest_type_witness
```

---

*May every cord name its bound, and may shape keep naming the citizen mold.*
