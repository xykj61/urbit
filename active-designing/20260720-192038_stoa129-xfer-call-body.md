# STOA129 — Xfer-Mold Flip Call-Body

*An Acme Corporation employee reading this gets the thinnest honest call-body deepen under a multi-face payload `$%` after STOA128: `sample=xfer-mold` through closed `%-  flip` on bartis and barket lowers as an XferMold switch with baked `.mint` from 5 amount 3 → `.send`, without ambient gold, argv under xfer, call-body under pair, or Sala B0.*

**Language:** EN  
**Version:** `20260720.192038` (UTC · cloud host)  
**Style:** Radiant (see `../context/RADIANT_STYLE.md`)  
**Voice:** Quin  
**Status:** Checkable — nest · bartis · barket witnesses bind xfer-flip accept  
**Register:** Checkable — `glow_nest_type_witness` · `glow_lower_bartis_witness` · `glow_barket_lower_witness`  
**Grounds in:** [`20260720-191033_stoa128-xact-call-body.md`](20260720-191033_stoa128-xact-call-body.md) · [`20260720-184953_stoa125-xfer-mold-gate.md`](20260720-184953_stoa125-xfer-mold-gate.md) · [`../glow/gen/mold-xfer.glow`](../glow/gen/mold-xfer.glow)

**dated_guard:** Do **not** edit the dated STOA128 wording artifact. Seat this lap in *this* brief and living pins when GREEN.

---

## Verdict (one breath)

**XFER_CALL_BODY.** Widen the closed call-body gate set for xfer-mold with **`flip`** — the thinnest nontrivial closed gate on `union(enum) { mint: struct { from: u32, amount: u32 }, send }`, mirroring the xact-mold flip shape. Parse admits `|=` / `|^` `sample=xfer-mold` / `%-  flip  sample`. Lower emits `XferMold` and a gate whose body is `switch (sample) { .mint => .send, .send => .{ .mint = .{ .from = 0, .amount = 0 } } }`; welcome main bakes `sample = .mint { from: 5, amount: 3 }` and checks the gate returns `.send`. `double` / `inc` / `dec` under xfer-mold still refuse. `flip` under pair-mold and `@u32` refuse. Call-body under pair, argv under any named mold, ambient gold, and Sala B0 all stay held.

---

## Why this is the honest next lean A

STOA128 closed the one-face payload `$%` call-body row. Remaining doors were ambient gold (port-order thick), argv under tagged/pair, triple-mold, and call-body under multi-face/struct molds. Xfer-mold already has a GREEN identity lower; splicing `flip` through the seated `.call_one` seam is thinner than gold and mirrors xact flip without inventing open polymorphism.

---

## What this lap lands (when GREEN)

| Item | Proof |
|------|-------|
| `is_xfer_call_gate` admits `flip` | `rune_bartis` · `rune_barket` |
| Xfer-mold `flip` lowers to `switch` body · baked `.send` | bartis · barket lower |
| `gate-xfer-flip.glow` · `gate-barket-xfer-flip.glow` welcome exit 0 | witnesses |
| `double` under xfer-mold still refuses | lower pins |
| `flip` under pair / `@u32` refuses | lower pins |

---

## What stays horizon

| Item | Why held |
|------|----------|
| `%-` call-body under `pair` | Struct arithmetic demos thicker |
| Argv under `xfer-mold` | `parseInt(u32)` wrong shape |
| Open `$:` beyond pair (`triple-mold`) | Thicker |
| Ambient gold | Port-order |
| Sala B0 | Framework / product season |

---

## Files / APIs to change

| Path | Change |
|------|--------|
| `glow/rune_bartis.rye` · `glow/rune_barket.rye` | `is_xfer_call_gate` |
| `glow/lower_bartis.rye` · `glow/lower_barket.rye` | `emit_xfer_call_body` · XferMold switch |
| `glow/gen/gate-xfer-flip.glow` · `gate-barket-xfer-flip.glow` | accept desks |
| `tools/session_logs_index_prepend.rish` · `tools/lexicon_barket_phrase_append.rish` | pin helpers (rish, not python) |
| witnesses · living pins · this brief | STOA129 GREEN when bound |

---

## Witness (after implement)

```bash
rishi/bin/rishi run tools/glow_nest_type_witness.rish
rishi/bin/rishi run tools/glow_barket_lower_witness.rish
rishi/bin/rishi run tools/glow_lower_bartis_witness.rish
```

---

*May every multi-face alphabet earn its one honest flip, and may gold wait until freer nesting asks for it.*
