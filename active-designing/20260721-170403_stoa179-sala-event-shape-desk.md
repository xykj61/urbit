# STOA179 — Sala-Event-Shape Desk

*An Acme Corporation employee reading this gets the first Glow OS citizen desk on the Sala B0 Glow ladder: a five-face `$:` mold whose face names match elder `receipt_core.FactFields`.*

**Language:** EN  
**Version:** `20260721.170403` (UDT · Framework host)  
**Style:** Radiant (see `../context/RADIANT_STYLE.md`)  
**Voice:** Quin  
**Status:** Checkable — bound by `glow_run`  
**Register:** Checkable (Two Rooms) — `sala-event-shape` → `SalaEventShape` GREEN · typed debt named · cast held  
**Grounds in:** [`20260721-165732_stoa178-sala-b0-glow-gap-inventory.md`](20260721-165732_stoa178-sala-b0-glow-gap-inventory.md) · [`../linengrow/receipt_core.rye`](../linengrow/receipt_core.rye) FactFields

**dated_guard:** Do **not** claim this desk produces the Sala demo root. Elder Rye fold stays. Stand-in `@u32` faces stay until `@t` (or peer) retires the debt. No cast twins this lap. No mold twin this lap. JABS3 stays held.

---

## Verdict (one breath)

**SALA_EVENT_SHAPE.** Seat `shape-sala-event.glow` with `+$ sala-event-shape` and faces `from` · `to` · `amount` · `memo` · `stamp`. Lower emits `SalaEventShape`. Faces use stand-in `@u32` so the desk fits today's nest/cast surface; real FactFields are byte slices — `@t` debt stays explicit.

---

## What this lap lands

| Item | Proof |
|------|-------|
| `shape-sala-event.glow` → `SalaEventShape` | `glow_run` |
| Desk witness wire | `glow_run_desk_witness` |
| Typed debt named | this brief · inventory |

---

## What stays next

| Item | Why |
|------|-----|
| **STOA180** `@t` / bounded cord (or peer) | retires stand-in on text faces |
| Cast twins for `sala-event-shape` | after types honest (or explicit stand-in cast) |
| Glow claim shell / sign·replay ports | later Phase G |
| JABS3 | companion check-in |

---

## Witness

```bash
rishi/bin/rishi run tools/glow_run.rish glow/gen/shape-sala-event.glow
```

---

*May every session event keep its five faces, and may stand-in auras never pretend to be the fold.*
