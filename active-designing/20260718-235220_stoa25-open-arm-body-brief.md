# STOA25 — Open arm body

**Status:** Checkable — thin face GREEN `20260718.235220`  
**Depth:** brief  
**Last updated:** 2026-07-18 (`20260718.235220`)  
**Voice:** Quin  
**Waymark:** **STOA** · compressor [`../docs/STOA.md`](../docs/STOA.md)

---

## Thin face claim

Exactly three Glow lines:

```
|%
++  id  sample
--
```

| Piece | Meaning |
|-------|---------|
| `++  id` | Arm name — any ident, not a closed demo gate |
| `sample` | Authored nest body (face) — lowers to `return sample;` |
| Welcome | `Core.id(5) == 5` |

Closed arms (`++  double` with empty body) keep the prior STOA14–20 path. Open bodies also accept a decimal literal (`++  five  5`). Nest forms `(mix …)` · `(jam …)` · casts stay later. Payload-face bodies parse when a payload is present (`++  id  amount`).

**Lower:** emit ordinary Rye method from the authored expr; welcome/unwelcome check the expect.

## Not in this face

Glow→Glow library import · nest bodies · conditionals inside arms · NativeActivity import.

---

*First authored `++` body — the language begins to write its own arms.*
