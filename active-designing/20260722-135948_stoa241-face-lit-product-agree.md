# STOA241 — Product face_lit Stand-In Agrees Glow face_lit

*An Acme Corporation employee reading this gets the thin door after Pixel eyes closed the lit-desk arc: the product names `extract_face_lit_cord` as its face=lit extract, and a dual witness proves that extract matches Glow `face_lit` on the fact-line desk through the same seated pins — without crossing the module-path import seam.*

**Language:** EN  
**Version:** `20260722.135948` (EDT · Framework host)  
**Style:** Radiant (see `../context/RADIANT_STYLE.md`)  
**Voice:** Quin  
**Status:** Checkable — GREEN `20260722.135948`  
**Register:** Checkable (Two Rooms) — product extract ≡ pins · Glow face_lit ≡ pins · no `@import` of glow/face_lit from linengrow  

**Grounds in:** STOA240 lean **B** (name next door → face_lit) · STOA233 desk emit · STOA219 Glow face_lit · STOA225/231 desk pins

**dated_guard:** Do **not** claim linengrow `@import`s `glow/face_lit`. Do **not** reopen pin/desk/embed/Pixel as unfinished. True shared-package import stays held. JABS3 held.

---

## Verdict (one breath)

**FACE_LIT_AGREE.** `extract_face_lit_cord` · `face_lit_product_agrees_pins` · Glow sample lits STOA241 · transitive pin agree across the seam.

---

## Seam named honestly

Zig forbids `linengrow` importing `../glow/face_lit` (same class as embed-outside-package). Product-local extract stays the runtime path; Glow `face_lit` stays the language-side parse. Both match the six seated face cords on the fact-line desk.

---

## Witness

```bash
export RYE_ZIG=vendor/zig-toolchain/zig
rishi/bin/rishi run tools/stoa241_face_lit_product_agree_witness.rish
```

---

## Hold

Shared package / true `@import` of Glow `face_lit` into the product. JABS3.
