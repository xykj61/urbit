# Cursorignore — Archive and Caches Out of the Index

*An Acme Corporation employee reading this gets the thin lean after the filetree check-in: Cursor’s session index was carrying closed archive shelves and host caches, so a root `.cursorignore` keeps those out of the editor’s memory while git continues to hold every dated file whole.*

**Language:** EN  
**Version:** `20260722.183653` (EDT · Framework host)  
**Style:** Radiant (see `../context/RADIANT_STYLE.md`)  
**Voice:** Quin  
**Status:** Checkable — `.cursorignore` seated with this lap  
**Register:** Checkable (Two Rooms) — ignore file on disk; indexing relief is host-side (restart Cursor if the index feels sticky)  

**Grounds in:** filetree append-front check-in `20260722.183514` lean **A** · ORGANIZING archive fold · one-clock / dated_guard  

**dated_guard:** Do **not** content-merge dated briefs or `.bron` logs. Do **not** delete `session-logs/archive/`. Ignore is indexing only.

---

## Verdict (one breath)

**CURSORIGNORE_THIN.** Root `.cursorignore` · archive shelves · build caches · vendor/elder trees · git still tracks what it already tracked.

---

## What is ignored (indexing only)

| Pattern family | Why |
|---|---|
| `session-logs/archive/` · other `**/archive/` shelves | Closed days — ~2k log files already folded |
| `tools/.cache/` · `tools/.build/` · `glow/.cache/` · `**/bin/` | Regenerated; huge on disk |
| `vendor/` · `old/` · `vere/` | Study / elder — open by path when needed |
| `.cursor-state/` · enclosure leftovers | Machine-local |

Living `session-logs/*.bron` (today’s flat day) and living `active-designing/` briefs stay indexable.

---

## Hold

| Door | State |
|---|---|
| Append-to-front content-merge | **held** |
| Yonder old designing by relevance (lean B) | await |
| Slower file birth (lean C) | await |

---

## After seating

Restart Cursor (or reload window) if the index still feels heavy — ignore files apply on the next index pass.

---

*May the pier keep every stamp, and the session carry only the living shelf.*
