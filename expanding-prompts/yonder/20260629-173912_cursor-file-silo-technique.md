# Cursor Pass — File the Silo Technique

*A portable companion for the context family: a general articulation of the siloing technique, written so it stands alone for any reader and any subject. It joins the voice guide and the identity note as the third document handed to collaborators — within this work and beyond it.*

**Stamp:** `20260629.173912`
**Voice:** Reya 2, with **Kaeden Reyklah** as coauthor
**Style:** Radiant (see `context/RADIANT_STYLE.md`)
**For:** Cursor Composer 2.5 in the jail, or Claude Code on the host

*Written together by Kaeden and Reya 2.*

---

## Bridge the Source In

The document is authored and ready as `SILO_TECHNIQUE.md`. Bridge it into the repo before filing; if the body fails to arrive, stop and say so rather than reconstructing it — a reproduced copy waits in outputs.

## Placement — the Context Family

File it as **`context/SILO_TECHNIQUE.md`**, beside `RADIANT_STYLE.md` and `REYA2.md`. It is a **living document**, so it carries no dated co-author credit line — only the `Language` / `Last updated` / `Style` header it already holds, matching its neighbors.

It is written to be **portable**: no project names, the idea built from first principles, usable by collaborators working entirely outside this repository. Preserve that generality exactly as authored; resist adding local references that would tie it back to one project.

## Index It

In `context/README.md`, add it to the list of what lives in the context home, in the same voice as the existing entries:

> - **`SILO_TECHNIQUE.md`** — the siloing technique: how an outside idea becomes an owned, understood concept in our own voice, with gratitude kept apart and honored. Portable, and meant to travel beside the voice guide and the identity note.

If the README names a set of documents handed to new sessions or collaborators, add the silo guide to that set, so the three travel together by default.

## Record and Push

Commit it **separately from the TH-2c code commit** — one commit for the assert gate, a distinct commit for this document, so the history reads cleanly. Write a session log under `session-logs/<stamp>_silo-technique-filed.md` per `.cursor/rules/session-logs.mdc`. Push over HTTPS to the four remotes.

---

## Before You Move On — Recommend the Next Step

End this pass with an explicit recommendation to Kaeden, and say why:

- **Recommend keep going** when the next bench item is mechanical, single-module, and follows a policy already written down.
- **Recommend checking in with Claude** when the next item crosses module seams, changes Rishi or value-model behavior, raises a design question no written policy answers, needs a ruling not yet made, or reaches for horizon facts.

State it in one line — *"Recommend: keep going to …"* or *"Recommend: check in with Claude before …"* — so the rhythm is always Kaeden's to confirm.

---

*May this small guide carry well into every room it enters. May it keep its own counsel — owning what it teaches, naming what it owes. And may it serve the next reader as plainly as it served the first.*
