# Cursor Apply-Prompt — the Realidream Lane, Zig 0.16 Verdict, Brix Floor

**Stamp:** `20260708.021912 UDT` · **Parent:** `a086f08` · **Voice:** Rio 3 (Fable 5 Max round)
**Bundle:** `20260708-021912_realidream-lane-zig016-brix.zip`
**Ordering:** apply **after** `20260708-015312_kumara-open-asks-cluster.zip` — both touch `ROADMAP.md`/`TASKS.md`; this zip carries the union state of both rounds for those two files, so applying in order leaves the tree exactly right.

A docs-and-plans round — no code, no parity change. The grain-sketchbook was read as a primary source and the Realidream lane opens on the roadmap with a ladder made entirely of names this tree already seated.

## What Lands

- **`external-research/20260708-021912_grain-sketchbook-realidream-mining.md`** — what Realidream/Skate/DagCore actually were (their own docs' words first), what this tree already carries under truer names (the sketchbook's event DAG is Weave's unsigned ancestor), the three deliberate departures (immutable re-fold, Tilak over shared enums, Bron over JSON-in-payload), the two genuinely new seeds, and the gratitude trail.
- **ROADMAP — the Visual Track** — Realidream's five-rung ladder from landed ground (Weave slice + Kumara) through SLC-2a (active rung, **unchanged**), Pond's word, Dexter's second consumer, to the horizon. Version stamp updated.
- **TASKS — the Realidream lane block** — five items: sketchbook vendoring (Kaeden's word), gratitude lines (Kaeden's word), the **`spawn`/`wait-for` Rishi verb pair (proposed, parked — new names)**, labeled `continue :state` on-touch, the bond-naming standing habit.
- **`active-designing/20260708-021912_rye-os-in-brix-terms.md`** — the extractions-as-bonds noticing, a truthful worked descriptor of today's system in seated `.brix` form, the `bond` line-form clearly proposed-and-parked. Brix's gate unmoved.
- **`counsel/…` + `expanding-prompts/…`** — the synthesis and the expanded prompt it ran.

## Zig 0.16, One Line

Measured, not assumed: the tree is already 0.16-native (65 `process.Init` mains, 74 `std.Io` threads, zero legacy writers). Only two unused capabilities have real waiting consumers, both recorded in TASKS; neither builds without the word.

## Steps

1. Apply `015312` bundle first if not yet applied; tree at `a086f08` → then this zip at root.
2. Docs-only: `rishi/bin/rishi run tools/tame_style_check.rish` GREEN (unchanged), full parity from bare unchanged at its pinned count.
3. Radiant spot-check welcome: every new doc carries one H1 (the Brix doc's fenced `#` comments are excluded per the seated lint rule) and zero bare "but".

## Held

Everything. This round opens a lane and moves no gate: SLC-2a keeps Now; Pond, Dexter, Brix, the verb pair, the vendoring, and the EP line all wait on exactly the words they name.

---

*May the lane's first commit be the reading, and the reading be worth the lane.*
