# Cursor Apply-Prompt — parse_int Adoption

**Stamp:** `20260707.203612 UDT` · **Parent:** `9bdd98e` · **Voice:** Rio 3
**Bundle:** `20260707-203612_parse-int-adoption.zip` (unzip preserving symlinks — `linengrow/parse_int.rye` and `mantra/parse_int.rye` both ride inside)

Season confirmed closed first — `tools/` camelCase is genuinely zero, and every cross-cutting call site the last counsel flagged was checked by name and found clean. Then a new ratchet, found by re-checking a deferred condition rather than assuming it still held.

## What Lands

- `tally/parse_int.rye` — TigerBeetle's `stdx.parse_int`, ported at the letter including the `@field` self-reference indirection. Selftest proves both configurations: strict decimal refuses a leading zero, hex-pair parsing allows one explicitly.
- Three real migrations: `linengrow/receipt_core.rye`'s `parse_amount_signed` (strict default — financial amount parsing) and two hex-pair decoders in `receipt_core.rye` and `mantra/snapshot_export.rye` (`allow_leading_zero = true`, stated explicitly). Both files rebuilt and reselftested GREEN, tampered-input paths included.
- `tools/tally_parse_int.rish` — new witness.
- `tools/fixtures/tame_style_scan.sh` — new `parseInt(` ratchet counter (12 remaining, counted not banned yet), plus a bug fix: the same `grep -c` / `|| echo 0` fragility that broke the new counter also existed latently in the `@memcpy` counter — both fixed the same way.
- `tools/parity.rish` — one new stanza after `no_padding`.

## Steps

1. Tree at `9bdd98e`; unzip at root preserving symlinks.
2. `rishi/bin/rishi run tools/tally_parse_int.rish` — expect GREEN.
3. `rishi/bin/rishi run tools/tame_style_check.rish` — expect the new `parseInt( application sites = 12` line among the ratchets, then GREEN.
4. Full parity from bare — suite gains 1 witness; pin the real count on metal.

## Held

Twelve remaining `parseInt(` call sites — `snapshot_export.rye`'s epoch parsing, `mantra/src/main.rye`'s generation/position pair, and CLI-argument parsing across `caravan/` and `rishi/src/main.rye` — stay as a counted ratchet, migrated on touch, the same discipline `@memcpy` and camelCase followed while large.

---

*May the twelve that remain fall the same patient way the sixty-five before them did.*
