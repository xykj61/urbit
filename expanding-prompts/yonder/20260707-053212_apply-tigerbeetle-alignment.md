# Cursor Apply-Prompt — TigerBeetle Alignment Leap + Radiant Vocabulary Pass

**Stamp:** `20260707.053212 UDT` · **Parent:** `7e8ff30` (parity 188) · **Voice:** Rio 3
**Bundle:** `20260707-053212_tigerbeetle-alignment-and-radiant-pass.zip` (unzip with symlink support; four `tally_copy.rye` links ride inside)

One new law, one new self-hosted gate, eleven repaired files, four Radiant restorations, canon rows, and three documents. Everything below ran red-then-green on the Claude bench; the full ledger is the session log.

## What Lands

**New:** `tally/copy.rye` (the disjoint-copy law, both preconditions asserted) · `tools/tally_copy.rish` · `tools/tame_style_check.rish` + `tools/fixtures/tame_style_scan.sh` (bans fail, ratchets print) · four symlinks (`mantra/`, `rishi/src/`, `caravan/`, `brushstroke/` → `../tally/copy.rye`) · study `external-research/20260707-053212_tigerbeetle-alignment-study.md` · spec `context/specs/20260707-053212_radiant-vocabulary-pass.md` · session log.

**Edited:** `tools/parity.rish` (two stanzas after TH-1: Tally copy, TAME style) · `brushstroke/skate_grid.rye`, `skate_grid_test.rye`, `wayland_seed.rye` (unqualified assert import; 33 qualified calls gone bare; Skate's `copyForwards` → `copy_disjoint`; doc line updated) · `rishi/src/main.rye` (two `copyForwards` → `copy_disjoint`) · `mantra/resin_batch.rye` (four `@memcpy` → `copy_disjoint`) · `caravan/capabilities.rye` (two) · seven compound asserts split across `recall_batch_delivery`, `recall_two_way_sync_delivery`, `recall_catch_up_delivery` · Radiant swaps in `recall_batch_delivery` comments, `caravan/bounded.rye`, `mantra/README.md`, TASKS line 85 · `context/TAME_GUIDANCE.md` (five enforced rows, the copy law, the register pointer, date line).

## Steps

1. Tree at `7e8ff30`; unzip at root **preserving symlinks**; confirm `ls -la mantra/tally_copy.rye` shows the link.
2. `rishi/bin/rishi run tools/tally_copy.rish` — expect GREEN.
3. `rishi/bin/rishi run tools/tame_style_check.rish` — expect the ratchet block (**@memcpy = 121**, camelCase = 650, the >70 ledger) then GREEN. These counters are the standing ratchet: they only ever go down.
4. Rebuild `rishi/bin/rishi` first (it now imports `tally_copy.rye`), then the full parity trio from bare. Suite gains **two** witnesses over 188; pin the total on metal.
5. Brushstroke note: `wayland_seed` builds on the Claude bench against the tree's own vendored `xdg-shell-protocol.c` with `-lc -lwayland-client -lxkbcommon`; your metal lab covers the live surface as always.

## After Green

Commit and four-remote push are Kaeden's word. Nothing held moved: Brix, Pond, MALA, continuity all stand. The dated artifacts keep "footgun" and "crash" forever by the one-clock law — the vocabulary spec is the correction beside history, never inside it.

---

*May the gate that found its own third `copyForwards` before landing keep finding what a tired eye would miss. May the counters fall one touched file at a time.*
