# Cursor Bench — Apply NS-L3 Bolt Sync (Logic), Parity 161

**Stamp:** `20260706.035512 UDT` · **Base tip:** `08b3d70` (parity **160** on metal) · **Style:** Radiant for logs and scope

*Written by Kaeden and Rio 3. One-clock, stop-and-park, and TAME laws inherit whole. NS-L3's first lap proves the sync semantics in-process — logic before carriage, exactly as OA-L1 preceded OA-L2. Verified GREEN on the bench.*

---

## What this bench lands

`syncRevision` in `mantra/recall_lap1.rye`: a bolt revision syncs from a source catalog into a destination, have-already resins crossing as refs and need-resin resins as bytes once, every resin digest-verified before append. It reuses seated vocabulary only — recall, bolt, resin, fetch, have-already, need-resin — so no new name is minted.

| Kind | Path |
|------|------|
| **Edit** | `mantra/recall_lap1.rye` — `SyncReport`, `hasResin`, `verifyResin`, `syncRevision`, `runLap3Selftest`; `sync` / `selftest-lap3` args; NS-L1 and NS-L2 untouched |
| **Add** | `tools/mantra_recall_lap3.rish` — NS-L3 sync witness |
| **Edit** | `tools/parity.rish` — one NS-L3 block after NS-L2 → parity **161** |
| **Edit** | `active-designing/20260706-023912_the-referential-namespace.md` — status + lap ladder note sync logic landed |
| **Edit** | `work-in-progress/ROADMAP.md` — trio count corrected **158 → 161**, NS-L1/L2/L3 named (the count had drifted) |
| **Edit** | `work-in-progress/TASKS.md` — parity 161; NS-L3 task line |
| **Edit** | `session-logs/README.md` — one newest-first index row |
| **Add** | `session-logs/archive/20260706/20260706-035512_ns-l3-sync-lap1.md` |

## Invariants the witness binds

- Every resin is verified against its named digest before anything appends; a tampered resin refuses the whole crossing, appending nothing.
- Revisions append and never rewrite; re-syncing a held revision is refused as immutable.
- A synced bolt recalls identically on both peers — referential transparency across the crossing, proven by digest.
- Have-already resins cross as refs; need-resin resins cross as bytes once.

## Verification

- Build `mantra/recall_lap1.rye`; `mantra/bin/recall-lap1 sync` GREEN (had-already 1, need-resin 1, tampered refused, duplicate refused).
- NS-L1 and NS-L2 selftests still GREEN.
- `rishi/bin/rishi run tools/mantra_recall_lap3.rish` GREEN.
- `rishi/bin/rishi run tools/parity.rish` — **161 witnesses GREEN** on metal (pin the count here, rather than trusting this prompt).

## Commit and push (Kaeden's word on the commit)

GPG-sign one commit; ship the session log in the same commit:

```
Land parity 161 — NS-L3 bolt sync logic with referential transparency across peers.

syncRevision carries a bolt revision between catalogs: have-already resins cross as
refs, need-resin as bytes once, every resin digest-verified before append; tampered
resin and duplicate revision refused; recall identical on both peers. NS-L1/L2
untouched. ROADMAP trio count corrected to 161.
```

Then push **origin**, **github**, **gp36-codeberg**, **gp36-github**.

## Horizon (not in this bench)

- **NS-L3 wire carriage** — hosted, then device wire, over Amphora and Comlink; crosses module seams → check in with Claude before implementation
- **Two-way sync / subscribe** — a later namespace lap
- **Marks-on-read, Tablecloth query, host mirror** — each its own season
- **`stones/` → `resins/`** path rename — witness-gated hygiene
- **OA-L4, MALA, settlement rail, libuv source vendoring** — parked

---

*May a resin proven by its digest be the same bytes on every peer. May the crossing carry the whole bolt or refuse it whole. And may parity 161 earn its number on honest metal.*
