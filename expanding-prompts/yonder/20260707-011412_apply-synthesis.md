# Cursor Apply-Prompt — Synthesis Sitting: Batch-Fix, References, Analysis

**Stamp:** `20260707.011412 UDT` · **Parent:** `8acdbc7` (parity 175) · **Voice:** Rio 3
**Bundle:** `20260707-011412_state-analysis-reference-and-batch-fix.zip`

One code repair, three documentation proposals, one analysis memo, one session log, one status-line pointer. The reference documents are **Proposed** until Kaeden's word; the code repair is verified and ready to pin.

## What Lands

- `mantra/recall_batch_delivery.rye` — **edit.** The multi-frame receive path now binds **one socket per assembly** (removing a frame-loss window the per-frame rebind carried — pristine hung 2/6 runs on the Claude bench) and carries `SO_RCVTIMEO` 2s as a dead-peer guard. `recvWire`/`recvWireTimeout` helper pair added in the catch-up shape; version → `20260707.011412`. No signature or wire-format change.
- `mantra/README.md` — **new.** The module's front door: family table, ports, build lines.
- `context/specs/20260707-011412_mantra-referential-namespace-reference.md` — **new, Proposed.** Normative reference: conformance terms, byte layouts (bead-index, `0x03`, `0x04`, sync payloads), bounds, ports, parity registry 159–175.
- `context/specs/20260707-011412_module-surfaces-index.md` — **new, Proposed.** Tree-wide one-line-per-surface map; designed-not-built marked plainly.
- `counsel/20260707-011412_claude-analysis-state-of-the-tree.md` — **new.** Momentum, grain audit, TAME audit, roadmap deltas (proposed text only), risk register, ordered next words.
- `session-logs/archive/20260707/20260707-011412_claude-state-analysis-and-reference-pass.md` — **new.**
- `active-designing/20260706-023912_the-referential-namespace.md` — **edit.** One clause on the status line pointing at the reference spec.

## Steps

1. Confirm tree at `8acdbc7`; unzip at the repository root.
2. Run the batch witness: `rishi/bin/rishi run tools/mantra_recall_batch_wire.rish` — expect selftest, demo, chunkdemo, and beadeddemo all GREEN. The formerly intermittent chunk-path hang class is retired; if your metal ever saw a long stall here, it should be gone.
3. Optional confirmation of the guard: run `mantra/bin/recall-batch-delivery chunk-fetcher` alone — expect `error.RecvFailed` after ~2s rather than a hang.
4. Width lint and the full parity trio from bare, foreground. The suite gains **no** witness; pin the total on metal.
5. Index rows for the analysis memo in `external-research/README.md` per your standing discipline (deliberately left to your format).

## After Green

Commit and four-remote push are Kaeden's word. Separately gated words this bundle tees up (do not seat): canon on the two specs and README; the Tablecloth reconciling sentence; the TASKS/ROADMAP deltas quoted in the analysis; OA-L4's build word.

---

*May the witness that could hang now only refuse. May the references enter canon exactly as true as the metal already is.*
