# Component — Fusion Design

**Purpose:** design how a Glow OS module fuses with an Urbit (or other) concept, in Glow, under the proposed vane-parallel names, governed by TAME — as research, not code.

**Tokens**

- `{{GLOW_MODULE}}` — the Glow OS module (e.g. Scribble, Comlink, Tally).
- `{{OTHER_CONCEPT}}` — the thing to fuse with (e.g. Hoon's marks, Clay, Ford, LMDB).
- `{{WHY_NOW}}` — the reason this fusion matters this season.

---

## The Expanded Prompt (fill the tokens, then run)

Design how **{{GLOW_MODULE}}** fuses with **{{OTHER_CONCEPT}}** for Glow OS, because **{{WHY_NOW}}**. Follow this fork's own discipline exactly:

1. **Read both sides first, from source.** Read `{{GLOW_MODULE}}`'s own README/code in this tree, and read `{{OTHER_CONCEPT}}` from `old/`, `vere/old/`, or a cited external source — never from memory. State what each side actually is before proposing anything.
2. **Propose the fusion in Glow, under proposed names.** Use the vane-parallel names from `context/specs/20260713-214400_urbit-parallel-naming-mapping-proposal.md` and the Glow language from the accepted-Glow spec. Every proposed role goes onto the names-awaiting-confirmation checklist.
3. **Keep TAME.** Every seam bounded and asserted; recursion named and bounded; a paired witness proposed for any correctness claim (the dual-description round-trip pattern).
4. **Name what stays open.** List the design forks that need Keaton's word; do not decide them unilaterally.
5. **Write no code.** This is a design doc in `external-research/` with a Pacific stamp; the first witness is named as a future step, not built.
6. **Address the generic Acme Corporation employee**, cite the silo not the held-whole original, and end with a Galaxy Pitch block if the fusion could matter past this fork.

**Produces:** one `external-research/<stamp>_<slug>.md` design doc; new rows on the names checklist; a bron session log; commits in CONTRIBUTING + Radiant style pushed to both remotes.
