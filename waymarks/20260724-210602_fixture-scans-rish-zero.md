# Fixture scans → `.rish` · `tools/*.py` zero

**Language:** EN  
**Stamp:** `20260724.210602`  
**Status:** Checkable — GREEN  
**Room:** Checkable  
**Witnesses:** `rishi/bin/rishi run tools/living_docs_lint.rish` · `rishi/bin/rishi run tools/dated_guard.rish` · `rishi/bin/rishi run tools/tame_style_scan_advise.rish`

---

## Claim

1. **`living_docs_lint_scan` migrated.** Entry is `tools/fixtures/living_docs_lint_scan.rish`; seven duties live in `living_docs_lint_scan.sh` via the run seam (`wc -c` · `grep` · `realpath`), matching the Python-at-seam craft note. Duty summaries match the retired `.py` baseline (duty7 now reports zero).
2. **`dated_guard_scan` migrated.** Entry is `tools/fixtures/dated_guard_scan.rish`; body `dated_guard_scan.sh`. Vacuous GREEN with no staged dated mods.
3. **`tools/*.py` count is zero.** Advise ratchet and living-docs duty7 both print target met. `classical-vedic-astrology/cast_a_chart.py` remains exempt by name outside `tools/`.

## SLC

- **Simple** — thin `.rish` · shell body · same outer wrappers  
- **Lovable** — big living docs never enter Rish line maps  
- **Complete** — both scans GREEN · count zero · law table updated  

## Next

Mand ring-2 and Hands We Grow writing seasons still wait on word.
