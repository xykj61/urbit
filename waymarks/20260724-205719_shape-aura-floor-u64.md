# Shape aura floor · `@u64` admitted

**Language:** EN  
**Stamp:** `20260724.205719`  
**Status:** Checkable — GREEN  
**Room:** Checkable  
**Witnesses:** `rishi/bin/rishi run tools/glow_rune_shape_witness.rish` · `rishi/bin/rishi run tools/glow_run.rish glow/gen/shape-stamp-aura.glow` · `rishi/bin/rishi run tools/glow_run.rish glow/gen/cast-named-stamp-aura.glow` · `rishi/bin/rishi run tools/glow_run.rish glow/gen/shape-stamp-mixed.glow`

---

## Claim

1. **Aura floor opens.** `@u64` joins the admitted `$:` / `$%` field-aura list in `glow/rune_shape.rye`. The list is **DATA in one place** (`admitted_shape_auras` · named bound `admitted_shape_aura_count`) — parse and lower both consult it (rung-1a lesson).
2. **Stamp aura is the first consumer.** `glow/gen/shape-stamp-aura.glow` declares `stamp=@u64` under `+$` / `$:` and round-trips green; same-desk named-cast twin `cast-named-stamp-aura.glow` greens beside it. Mixed desk `shape-stamp-mixed.glow` keeps `@u64` with `@u32` and `@t`.
3. **The wall stays.** Genuinely unsupported auras still refuse with `UnsupportedShapeAura` — `rune_shape_witness` asserts the refuse for `@u8`. Widening the gate does not open the wall.
4. **Existing shape desks stay green** — amount · pair · xfer · seva-event · fact-line · named-cast amount — no rewrite required.

## SLC

- **Simple** — one table · one refuse · one first consumer  
- **Lovable** — Decision 6 stamp face now speaks through product shapes  
- **Complete** — positive · mixed · negative · elder desks GREEN  

## Next

`.py` ratchet advise · stranger's lap witness (OPEN_QUESTIONS #4). Mand ring-2 still on word.
