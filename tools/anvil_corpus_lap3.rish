# anvil_corpus_lap3.rish — Anvil corpus lap 2 max_lines filter witness.
#
# Run from the repository root:
#   rishi/bin/rishi run tools/anvil_corpus_lap3.rish

let zig = "vendor/zig-toolchain/zig"
let bin = "anvil/bin/anvil"

let build = run ["sh" "-c" "mkdir -p anvil/bin && env RYE_ZIG=${zig} rye/bin/rye build anvil/anvil.rye -femit-bin=${bin}"]
assert build.ok else "Anvil corpus lap 3 build failed"

let selftest = run ["sh" "-c" "${bin} selftest 2>&1"]
assert selftest.ok else "Anvil selftest exited non-zero"
assert selftest.out contains "GREEN" else "Anvil selftest not GREEN"
assert selftest.out contains "max_lines filter" else "Anvil missing max_lines welcome"

say "GREEN: Anvil corpus lap 3 — max_lines filter on corpus query."
