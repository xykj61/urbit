# seva_surface_witness.rish — Seva R2: the unified viewer, many agents on one surface.
#
# The Landscape-parallel proper: ask, receipt, reputation — each a
# capability-isolated Pool-hosted agent — gathered onto one Seva surface,
# each rendered as its own bounded fold. Also the second consumer that
# graduates Dexter. See
# external-research/20260715-172000_pool-keystone-revival-realidream-landscape.md
# and linengrow/seva_surface.rye.
#
# Run from the repository root:
#   rishi/bin/rishi run tools/seva_surface_witness.rish

let zig = "vendor/zig-toolchain/zig"
let bin = "linengrow/bin/seva_surface_witness"

let build = run ["sh" "-c" "mkdir -p linengrow/bin && env RYE_ZIG=${zig} rye/bin/rye build linengrow/seva_surface_witness.rye -femit-bin=${bin}"]
assert build.ok else "Seva R2 surface witness build failed"

let selftest = run ["sh" "-c" "${bin} 2>&1"]
assert selftest.ok else "Seva R2 surface witness exited non-zero"
assert selftest.out contains "GREEN" else "Seva R2 surface witness not GREEN"
assert selftest.out contains "three agents on one surface" else "Seva R2 missing the unified-surface welcome path"
assert selftest.out contains "agree byte-for-byte" else "Seva R2 missing the determinism claim"
assert selftest.out contains "shows only what Pool admitted" else "Seva R2 missing the hosted-only unwelcome path"

say "GREEN: Seva R2 unified-viewer witness passed."
