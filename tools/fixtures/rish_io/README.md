# Rishi file I/O witness scratch

The witness at `tools/rish_file_io_witness.rish` writes `roundtrip.txt` and `number.txt` here during each run. Generated `*.txt` files are gitignored; the directory itself is the fixture anchor.
