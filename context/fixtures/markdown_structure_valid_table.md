# Valid four-column table fixture

**Language:** EN  
**Status:** Fixture — must PASS markdown_structure_witness

| Col A | Col B | Col C | Col D |
|---|---|---|---|
| one | two | three | four |

Also valid with pipes inside inline code: `a|b|c` must not split cells.

| Key | Value (example `x|y`) | Unit | Notes |
|---|---|---|---|
| alpha | `pipe|inside|code` | count | GREEN welcome path |
