# Weave Slice — Kaeden's Ruling

**Stamp:** `20260707.030412 UDT` (Kaeden's clock, following counsel `025212`)
**Language:** EN
**Style:** Radiant (see `../RADIANT_STYLE.md`)
**Status:** Seated — Kaeden's word; closes snapshot "Weave slice" with prose only

*Written by Kaeden and Rio 3.*

---

## The ruling

**Sovereign "Weave slice" means what I2 already exports today.**

`BoltCatalog`'s own accretion — every leaf, every revision, the SHA3-512 HEAD record proving replay — is the Weave slice in every sense that has mattered across the sovereign-snapshot arc. The I2 `catalog/` bundle at parity **180**→**182** carries it; no separate `weave/` bundle is required.

## What the namespace brief meant

The referential-namespace brief's line — *"The history is a Weave — each revision of a bolt is a fact folded onto the append-only DAG"* — names the **metaphor** for bolt revision history as it lives in `BoltCatalog`. It is not a load-bearing dependency on `mantra/src/weave.rye`, which remains the text-buffer line-history aspect under `diff.rye` until the graduation trigger fires.

Rebuilding `BoltCatalog` to rest on the literal text-history file would be real work when something needs it; nothing in the current arc does.

## What stays held

Per counsel [`20260707-025212`](../counsel/20260707-025212_claude-counsel-weave-brix-pond-customs.md):

| Item | Status |
|------|--------|
| **Brix closure in snapshot** | Held — Brix's own first lap |
| **Pond customs on import** | **Landed** `20260710.161125` — `pond/customs.rye`; place / hold / refuse; Amphora receipt gated; parity **230** |
| **Continuity custody** (live-instance + per-host epoch) | Kaeden gate — lap 2 structural check only |

---

*May the name resolve before the code that would need it. May the catalog's accretion carry the slice whole, and may every other room wait for its own ground.*
