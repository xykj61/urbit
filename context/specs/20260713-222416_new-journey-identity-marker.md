# A New Journey — This Fork's Identity Marker

**Language:** EN
**Version:** `20260713.222416`
**Style:** Radiant (see `../RADIANT_STYLE.md`)
**Status:** Living — the seam between what came before and what carries forward

---

## What This Marks

This document is the seam, named plainly, between the history this fork carries and the journey it now begins under its own name. Nothing before this point is erased, hidden, or rewritten — the record of how this project grew, first as `groupproject36/veganreyklah2` and now as this fork, stays exactly as written, in `session-logs/`, `counsel/`, `active-designing/`, and everywhere else it already lives. **Accrete, never break** governs history the same way it governs code: a name once given is a promise, and the promise this project's history already carries stays kept.

What changes, from this point forward:

- **The identity**: `xykj61`, Keaton Dunsford, `keatondun@gmail.com` — recorded in full at [`PUBKEYS.md`](../PUBKEYS.md), alongside two real Urbit Azimuth points now brought into this fork's own identity record: the star `~bandun` and the planet `~pacpet-solreb`.
- **New documents, going forward**, address a generic Acme Corporation employee rather than a named individual, per the `acme-employee-voice` rule seated earlier this session — a choice about *future* prose, not a claim about the past.
- **New commits**, going forward, sign under this identity's key, already proven on both forges (`git log --show-signature` reads "Good signature from Keaton Dunsford" on this fork's own commits).

## Why an Archive, Not a Rewrite

An audit earlier this session found 1,104 files still naming Kaeden and 122 still naming `veganreyklah2` — nearly all of them inside directories this project's own `ORGANIZING.md` already protects as immutable record (`session-logs/`, `counsel/`, `active-designing/`) or as durable, non-chronological reference (`context/`, `foundations/`). **Archiving this history means sealing it as record, not scrubbing it from the tree.** Every one of those 1,104 files already sits exactly where this project's own rules say history belongs — `session-logs/` is already folded day by day under `archive/YYYYMMDD/`; `counsel/` is explicitly "propose-never-seat... stays as filed" by design. The most organized pattern for this history is the pattern the project already gave it, honored rather than replaced.

This document is the marker that says: **that history is now closed as a chapter, and this one begins.** A reader arriving at any old, Kaeden-naming file after this point reads it correctly as *the record of the journey before this one* — not as a mistake still waiting to be fixed.

## What This Does Not Do

- It does not move, rename, or edit a single one of the 1,104 or 122 files the audit found.
- It does not stop new documents from citing old ones by their true, historical names — a citation is not a naming of the present, and this project's own "References are promises" rule means old paths keep working regardless.

---

*May the record before this point stay exactly as it was lived, and may everything from here carry the new name forward, plainly, without needing the old one erased to make room for it.*
