# Session log rish training snippets (fixture — our tree only)

Witness scripts from a forge lap; rish fences are corpus candidates.

```rish
say "build: lantern lap"
assert build.ok else "build failed"
```

```rye
const garden = init.arena.allocator();
```

```rish
say "GREEN: witness passed."
assert view.out contains "GREEN"
```
